(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[974],{1610:()=>{},3583:()=>{},6284:(e,n,t)=>{"use strict";t.d(n,{default:()=>ne});var i={};t.r(i),t.d(i,{CUMSUM3D:()=>em,CUMSUMReduction:()=>Q,Correlation2D:()=>ex,CorrelationConvolution:()=>ed,CovarianceConvolution:()=>ec,CovarianceReduction:()=>K,LinearSlopeReduction:()=>$,MaxConvolution:()=>er,MaxConvolution2D:()=>eu,MaxReduction:()=>W,MeanConvolution:()=>et,MeanConvolution2D:()=>eo,MeanReduction:()=>Z,MinConvolution:()=>ei,MinConvolution2D:()=>el,MinReduction:()=>q,StDevConvolution:()=>ea,StDevConvolution2D:()=>es,StDevReduction:()=>H,TwoVarLinearSlopeConvolution:()=>ef,TwoVarLinearSlopeReduction:()=>J});var r={};t.r(r),t.d(r,{CUMSUM3D:()=>eB,CUMSUMReduction:()=>ez,Correlation2D:()=>eV,CorrelationConvolution:()=>eX,CovarianceConvolution:()=>eA,CovarianceReduction:()=>eb,LinearSlopeReduction:()=>eh,MaxConvolution:()=>eC,MaxConvolution2D:()=>eY,MaxReduction:()=>eg,MeanConvolution:()=>eI,MeanConvolution2D:()=>eO,MeanReduction:()=>ey,MinConvolution:()=>eD,MinConvolution2D:()=>eP,MinReduction:()=>eS,StDevConvolution:()=>eM,StDevConvolution2D:()=>ej,StDevReduction:()=>ev,TwoVarLinearSlopeConvolution:()=>eL,TwoVarLinearSlopeReduction:()=>e_});var a=t(3013),o=t(597),l=t(5436),u=t(5999),s=t(9841);let d=" // by Jeran Poehls\r\n\r\nout vec3 vOrigin;\r\nout vec3 vDirection;\r\nout vec3 aPosition;\r\n\r\nout vec2 Vuv;\r\n\r\nvoid main() {\r\n    vec4 worldPos = modelViewMatrix * vec4( position, 1.0 );\r\n\r\n    aPosition = position; //Pass out position for sphere frag\r\n    vOrigin = vec3( inverse( modelMatrix ) * vec4( cameraPosition, 1.0 ) ).xyz;\r\n    vDirection = position - vOrigin;\r\n    Vuv = uv;\r\n    gl_Position = projectionMatrix * worldPos;\r\n}";var c=t(2980),f=t(2583),m=t(979);let x=e=>{let{volTexture:n}=e,{shape:t,colormap:i,flipY:r}=(0,c.on)((0,f.k)(e=>({shape:e.shape,colormap:e.colormap,flipY:e.flipY}))),{valueRange:l,xRange:u,yRange:x,zRange:p,quality:y,animProg:S,cScale:g,cOffset:v,useFragOpt:z,transparency:h,nanTransparency:_,nanColor:b,vTransferRange:k,vTransferScale:w}=(0,c.Ws)((0,f.k)(e=>({valueRange:e.valueRange,xRange:e.xRange,yRange:e.yRange,zRange:e.zRange,quality:e.quality,animProg:e.animProg,cScale:e.cScale,cOffset:e.cOffset,useFragOpt:e.useFragOpt,transparency:e.transparency,nanTransparency:e.nanTransparency,nanColor:e.nanColor,vTransferRange:e.vTransferRange,vTransferScale:e.vTransferScale}))),I=t.y/t.x,D=(0,s.useMemo)(()=>new o.BKk({glslVersion:o.Wdf,uniforms:{map:{value:n},cmap:{value:i},cOffset:{value:v},cScale:{value:g},threshold:{value:new o.I9Y(l[0],l[1])},scale:{value:t},flatBounds:{value:new o.IUQ(-u[1],-u[0],p[0],p[1])},vertBounds:{value:new o.I9Y(x[0]*I,x[1]*I)},steps:{value:y},animateProg:{value:S},transparency:{value:h},opacityMag:{value:w},useClipScale:{value:k},nanAlpha:{value:1-_},nanColor:{value:new o.Q1f(b)}},vertexShader:d,fragmentShader:z?"// This is an optimized version of the original Volumetric shader\r\n\r\nprecision highp float;\r\nprecision highp sampler3D;\r\n\r\nin vec3 vOrigin;\r\nin vec3 vDirection;\r\n\r\nout vec4 color;\r\n\r\nuniform sampler3D map;\r\nuniform sampler2D cmap;\r\n\r\nuniform float cOffset;\r\nuniform float cScale;\r\nuniform vec3 scale;\r\nuniform vec2 threshold;\r\nuniform float steps;\r\nuniform vec4 flatBounds;\r\nuniform vec2 vertBounds;\r\nuniform float animateProg;\r\nuniform float transparency;\r\nuniform float nanAlpha;\r\nuniform vec3 nanColor;\r\n\r\nvec2 hitBox(vec3 orig, vec3 dir) {\r\n    vec3 box_min = vec3(-(scale * 0.5));\r\n    vec3 box_max = vec3(scale * 0.5);\r\n    vec3 inv_dir = 1.0 / dir;\r\n    vec3 tmin_tmp = (box_min - orig) * inv_dir;\r\n    vec3 tmax_tmp = (box_max - orig) * inv_dir;\r\n    vec3 tmin = min(tmin_tmp, tmax_tmp);\r\n    vec3 tmax = max(tmin_tmp, tmax_tmp);\r\n    float t0 = max(tmin.x, max(tmin.y, tmin.z));\r\n    float t1 = min(tmax.x, min(tmax.y, tmax.z));\r\n    return vec2(t0, t1);\r\n}\r\n\r\nfloat sample1( vec3 p ) {\r\n    return texture( map, p ).r;\r\n}\r\n\r\n#define epsilon 0.0001\r\n\r\nvoid main() {\r\n    vec3 rayDir = normalize(vDirection);\r\n    vec2 bounds = hitBox(vOrigin, rayDir);\r\n\r\n    if (bounds.x > bounds.y) discard;\r\n\r\n    bounds.x = max(bounds.x, 0.0);\r\n\r\n    vec3 p = vOrigin + bounds.x * rayDir;\r\n    vec3 inc = 1.0 / abs(rayDir);\r\n\r\n    //Step Sizes\r\n    float fineDelta = min(inc.x, min(inc.y, inc.z)) / steps;\r\n\r\n    float coarseDelta = min(inc.x, min(inc.y, inc.z))/50.;\r\n\r\n    float delta = fineDelta;\r\n\r\n    vec4 accumColor = vec4(0.0);\r\n    float alphaAcc = 0.0;\r\n\r\n    float t = bounds.x;\r\n    int countdown = 0;\r\n    bool useCoarseStep = false;\r\n\r\n    while (t < bounds.y) {\r\n        vec3 p = vOrigin + rayDir * t;\r\n        \r\n        // --- Boundary checks ---\r\n        if (p.x < flatBounds.x || p.x > flatBounds.y ||\r\n            p.z < flatBounds.z || p.z > flatBounds.w ||\r\n            p.y < vertBounds.x || p.y > vertBounds.y) {\r\n\r\n            t += useCoarseStep ? coarseDelta : fineDelta;\r\n            continue;\r\n        }\r\n\r\n        vec3 texCoord = p / scale + 0.5;\r\n        texCoord.z = mod(texCoord.z + animateProg, 1.0001);\r\n        float d = sample1(texCoord);\r\n\r\n        bool cond = nanAlpha == 0. ? (d > threshold.x) && (d < threshold.y) : (d > threshold.x) && (d < threshold.y+.01); //We skip over nans if the transparency is enabled\r\n        \r\n        if (cond) {\r\n            // Hit something interesting - switch to fine stepping\r\n            if (useCoarseStep) {\r\n                useCoarseStep = false;\r\n                countdown = 40;\r\n                // Step back to ensure we don't miss the boundary\r\n                t -= coarseDelta;\r\n                continue;\r\n            }\r\n            if (d == 1.){\r\n                accumColor.rgb += (1.0 - alphaAcc) * pow(nanAlpha, 5.) * nanColor.rgb;\r\n                alphaAcc += pow(nanAlpha, 5.);\r\n            }\r\n            else{\r\n                float sampLoc = (d - 0.5)*cScale + 0.5;\r\n                sampLoc = min(sampLoc+cOffset,0.99);\r\n                vec4 col = texture(cmap, vec2(sampLoc, 0.5));\r\n                float alpha = pow(max(sampLoc, 0.001), transparency);\r\n                accumColor.rgb += (1.0 - alphaAcc) * alpha * col.rgb;\r\n                alphaAcc += alpha * (1.0 - alphaAcc);\r\n            }      \r\n\r\n            if (alphaAcc >= 1.0) break;\r\n            \r\n            t += fineDelta;\r\n        }\r\n        else {\r\n            // Nothing interesting here\r\n            if (countdown > 0) {\r\n                countdown--;\r\n                t += fineDelta; // Continue with fine steps while countdown > 0\r\n            } else {\r\n                useCoarseStep = true;\r\n                t += coarseDelta; // Switch to coarse steps\r\n            }\r\n        }\r\n    }\r\n    accumColor.a = alphaAcc; // Set the final accumulated alpha\r\n    color = accumColor;\r\n    if (color.a == 0.0) discard;\r\n}\r\n":"// by Jeran Poehls\r\nprecision highp float;\r\nprecision highp sampler3D;\r\n\r\nin vec3 vOrigin;\r\nin vec3 vDirection;\r\n\r\nout vec4 color;\r\n\r\nuniform sampler3D map;\r\nuniform sampler2D cmap;\r\n\r\nuniform float cOffset;\r\nuniform float cScale;\r\nuniform vec3 scale;\r\nuniform vec2 threshold;\r\nuniform float steps;\r\nuniform vec4 flatBounds;\r\nuniform vec2 vertBounds;\r\nuniform float animateProg;\r\nuniform float transparency;\r\nuniform float nanAlpha;\r\nuniform vec3 nanColor;\r\nuniform float opacityMag;\r\nuniform bool useClipScale;\r\n\r\n\r\nvec2 hitBox(vec3 orig, vec3 dir) {\r\n    vec3 box_min = vec3(-(scale * 0.5));\r\n    vec3 box_max = vec3(scale * 0.5);\r\n    vec3 inv_dir = 1.0 / dir;\r\n    vec3 tmin_tmp = (box_min - orig) * inv_dir;\r\n    vec3 tmax_tmp = (box_max - orig) * inv_dir;\r\n    vec3 tmin = min(tmin_tmp, tmax_tmp);\r\n    vec3 tmax = max(tmin_tmp, tmax_tmp);\r\n    float t0 = max(tmin.x, max(tmin.y, tmin.z));\r\n    float t1 = min(tmax.x, min(tmax.y, tmax.z));\r\n    return vec2(t0, t1);\r\n}\r\n\r\nfloat sample1( vec3 p ) {\r\n    return texture( map, p ).r;\r\n}\r\n\r\n#define epsilon 0.0001\r\n\r\nvoid main() {\r\n    vec3 rayDir = normalize(vDirection);\r\n    vec2 bounds = hitBox(vOrigin, rayDir);\r\n\r\n    if (bounds.x > bounds.y) discard;\r\n\r\n    bounds.x = max(bounds.x, 0.0);\r\n\r\n    vec3 p = vOrigin + bounds.x * rayDir;\r\n    vec3 inc = 1.0 / abs(rayDir);\r\n    float delta = min(inc.x, min(inc.y, inc.z));\r\n    delta /= steps;\r\n    vec4 accumColor = vec4(0.0);\r\n    float alphaAcc = 0.0;\r\n\r\n    for (float t = bounds.x; t < bounds.y; t += delta) {\r\n        p = vOrigin + rayDir * t;\r\n        if (p.x > -flatBounds.x || p.x < -flatBounds.y) { \r\n            continue;\r\n        }\r\n        if (-p.z > -flatBounds.z || -p.z < -flatBounds.w) {\r\n            continue;\r\n        }\r\n        if (p.y < vertBounds.x || p.y > vertBounds.y) {\r\n            continue;\r\n        }\r\n\r\n        vec3 texCoord = p / scale + 0.5;\r\n        texCoord.z = mod(texCoord.z + animateProg, 1.0001);\r\n        float d = sample1(texCoord);\r\n\r\n        bool cond = nanAlpha == 0. ? (d >= threshold.x) && (d <= threshold.y) : (d >= threshold.x) && (d <= threshold.y); //We skip over nans if the transparency is enabled\r\n        \r\n        if (cond) {\r\n            if (d == 1.){\r\n                accumColor.rgb += (1.0 - alphaAcc) * pow(nanAlpha, 5.) * nanColor.rgb;\r\n                alphaAcc += pow(nanAlpha, 5.);\r\n            }\r\n            else{\r\n                float sampLoc = (d - 0.5)*cScale + 0.5;\r\n                sampLoc = min(sampLoc+cOffset,0.99);\r\n                vec4 col = texture(cmap, vec2(sampLoc, 0.5));\r\n                float alpha;\r\n                if (useClipScale){\r\n                    float normalizedOpacity = clamp((sampLoc - threshold.x) / (threshold.y - threshold.x), 0.0, 1.0);\r\n                    alpha = pow(max(normalizedOpacity, 0.001), transparency*opacityMag);\r\n                } else {\r\n                    alpha = pow(max(sampLoc, 0.001), transparency*opacityMag);\r\n                }\r\n                accumColor.rgb += (1.0 - alphaAcc) * alpha * col.rgb;\r\n                alphaAcc += alpha * (1.0 - alphaAcc);\r\n            }      \r\n\r\n            if (alphaAcc >= 1.0) break;\r\n        }\r\n    }\r\n\r\n    accumColor.a = alphaAcc; // Set the final accumulated alpha\r\n    color = accumColor;\r\n    if (color.a == 0.0) discard;\r\n}\r\n",transparent:!0,blending:o.NTi,depthWrite:!1,side:o.hsX}),[z]),C=(0,s.useMemo)(()=>new o.WBB(2,4),[]);return(0,s.useEffect)(()=>{if(D){let e=D.uniforms;e.map.value=n,e.cmap.value=i,e.cOffset.value=v,e.cScale.value=g,e.threshold.value.set(l[0],l[1]),e.scale.value=t,e.flatBounds.value.set(-u[1],-u[0],p[0],p[1]),e.vertBounds.value.set(x[0]*I,x[1]*I),e.steps.value=y,e.animateProg.value=S,e.transparency.value=h,e.nanAlpha.value=1-_,e.nanColor.value.set(b),e.opacityMag.value=w,e.useClipScale.value=k,(0,m.m)()}},[n,t,i,v,g,l,u,x,p,I,y,S,h,_,b,w,k]),(0,a.jsx)(a.Fragment,{children:(0,a.jsx)("mesh",{geometry:C,scale:[1,r?-1:1,1],children:(0,a.jsx)("primitive",{attach:"material",object:D})})})};var p=t(3541),y=t(3947);let S=e=>{let{ZarrDS:n}=e,{setTimeSeries:t,setPlotDim:i,setDimCoords:r,updateTimeSeries:l,updateDimCoords:u}=(0,c.on)((0,f.k)(e=>({setTimeSeries:e.setTimeSeries,setPlotDim:e.setPlotDim,setDimCoords:e.setDimCoords,updateTimeSeries:e.updateTimeSeries,updateDimCoords:e.updateDimCoords}))),{analysisMode:d,analysisArray:m}=(0,c.r2)((0,f.k)(e=>({analysisMode:e.analysisMode,analysisArray:e.analysisArray}))),{shape:x,dataShape:S,strides:g,dimArrays:v,dimNames:z,dimUnits:h}=(0,c.on)((0,f.k)(e=>({shape:e.shape,dataShape:e.dataShape,strides:e.strides,dimArrays:e.dimArrays,dimNames:e.dimNames,dimUnits:e.dimUnits}))),{selectTS:_,getColorIdx:b,incrementColorIdx:k}=(0,c.Ws)((0,f.k)(e=>({selectTS:e.selectTS,getColorIdx:e.getColorIdx,incrementColorIdx:e.incrementColorIdx}))),w=(0,s.useRef)(0),I=(0,s.useMemo)(()=>new o.iNn(1,1,1),[]);return(0,s.useEffect)(()=>()=>{I.dispose()},[]),(0,a.jsx)(a.Fragment,{children:(0,a.jsx)("mesh",{geometry:I,scale:x,onClick:e=>{e.stopPropagation(),e.intersections.length>0&&_&&function(e){e.point;let a=e.uv,o=e.normal,s=(0,p._8)(o);if(s!=w.current&&(t({}),r({})),w.current=s,n){var c,f;let e=(0,p.PE)({data:d?m:(0,p.Td)(),shape:S,stride:g},{uv:a,normal:o}),n=o.toArray().map((e,n)=>Math.abs(e)>0?n:null).filter(e=>null!==e);i(2-n[0]);let t=(0,p.jT)({normal:o,uv:a}).map((e,n)=>e?v[n][Math.round(e*v[n].length)]:null),r=z.filter((e,n)=>null!==t[n]),s=h.filter((e,n)=>null!==t[n]);t=t.filter(e=>null!==e);let x="".concat(t[0],"_").concat(t[1]);l({[x]:{color:(0,y.md)(b()/10,"Paired"),data:e}}),k(),u({[x]:{first:{name:r[0],loc:null!=(c=t[0])?c:0,units:s[0]},second:{name:r[1],loc:null!=(f=t[1])?f:0,units:s[1]},plot:{units:h[2-n[0]]}}})}}(e.intersections[0])},children:(0,a.jsx)("meshBasicMaterial",{transparent:!0,opacity:0})})})},g=e=>{let{dimensions:n,ZarrDS:t,setters:i}=e,{width:r,height:o,depth:l}=n,{setPoints:u,setStride:d,setDimWidth:m}=i,x=(0,c.Ws)(e=>e.selectTS),S=r/o,g=l/o,{dimArrays:v,dimUnits:z,dimNames:h,strides:_,dataShape:b,setPlotDim:k,setTimeSeries:w,updateTimeSeries:I,setDimCoords:D,updateDimCoords:C}=(0,c.on)((0,f.k)(e=>({dimArrays:e.dimArrays,dimUnits:e.dimUnits,dimNames:e.dimNames,strides:e.strides,dataShape:e.dataShape,setPlotDim:e.setPlotDim,setTimeSeries:e.setTimeSeries,updateTimeSeries:e.updateTimeSeries,setDimCoords:e.setDimCoords,updateDimCoords:e.updateDimCoords}))),{analysisMode:M,analysisArray:O}=(0,c.r2)((0,f.k)(e=>({analysisMode:e.analysisMode,analysisArray:e.analysisArray}))),P=(0,s.useRef)(null),{timeScale:Y,getColorIdx:j,incrementColorIdx:X}=(0,c.Ws)((0,f.k)(e=>({timeScale:e.timeScale,getColorIdx:e.getColorIdx,incrementColorIdx:e.incrementColorIdx})));return(0,a.jsxs)("mesh",{scale:[S,1,g*Y],onClick:function(e){if(!x)return;let n=e.uv,i=e.normal,a=(0,p._8)(i);if(a!=P.current&&(w({}),D({}),u({})),P.current=a,t){var s,c;let e=(0,p.PE)({data:M?O:(0,p.Td)(),shape:b,stride:_},{uv:n,normal:i}),t=i.toArray().map((e,n)=>Math.abs(e)>0?n:null).filter(e=>null!==e);k(2-t[0]);let a=(0,p.jT)({normal:i,uv:n}),f=a.map((e,n)=>e?v[n][Math.round(e*v[n].length-.5)]:null),x=h.filter((e,n)=>null!==f[n]),S=z.filter((e,n)=>null!==f[n]);f=f.filter(e=>null!==e);let g="".concat(f[0],"_").concat(f[1]),w={color:(0,y.md)(j()/10,"Paired"),data:e};X(),I({[g]:w}),C({[g]:{first:{name:x[0],loc:null!=(s=f[0])?s:0,units:S[0]},second:{name:x[1],loc:null!=(c=f[1])?c:0,units:S[1]},plot:{units:z[2-t[0]]}}});let D=[l,o,r].filter((e,n)=>null!=a[n]),P=[l,o,r].filter((e,n)=>null==a[n]),Y=a.filter(e=>null!=e),A=_.filter((e,n)=>null!=a[n]),L=Math.round(Y[0]*D[0]-.5),B=Math.round(Y[1]*D[1]-.5),V=L*A[0]+B*A[1],N=_.filter((e,n)=>null==a[n]);m(P[0]),u(e=>({...{[g]:V},...e})),d(N[0])}},children:[(0,a.jsx)("boxGeometry",{}),(0,a.jsx)("meshBasicMaterial",{transparent:!0,opacity:0})]})},v=e=>{let{textures:n,ZarrDS:t}=e,{texture:i,colormap:r}=n,{timeSeries:l,flipY:u}=(0,c.on)((0,f.k)(e=>({timeSeries:e.timeSeries,flipY:e.flipY}))),{scalePoints:d,scaleIntensity:m,pointSize:x,cScale:p,cOffset:y,valueRange:S,animProg:v,selectTS:z,timeScale:h,xRange:_,yRange:b,zRange:k}=(0,c.Ws)((0,f.k)(e=>({scalePoints:e.scalePoints,scaleIntensity:e.scaleIntensity,pointSize:e.pointSize,cScale:e.cScale,cOffset:e.cOffset,valueRange:e.valueRange,animProg:e.animProg,selectTS:e.selectTS,timeScale:e.timeScale,xRange:e.xRange,yRange:e.yRange,zRange:e.zRange})));(0,c.$U)(e=>e.setError);let[w,I]=(0,s.useState)({}),[D,C]=(0,s.useState)(Array(10).fill(-1)),[M,O]=(0,s.useState)(1),[P,Y]=(0,s.useState)(0);(0,s.useEffect)(()=>{let e=Object.keys(w),n=Object.keys(l),t=(e=e.filter(e=>n.includes(e))).map(e=>w[e]);C([...t,...Array(Math.max(0,10-t.length)).fill(-1)])},[l,w]);let{data:j,width:X,height:A,depth:L}=(0,s.useMemo)(()=>i instanceof o.dYF?{data:i.image.data,width:i.image.width,height:i.image.height,depth:i.image.depth}:(console.warn("Provided texture is not a Data3DTexture"),{data:[],width:0,height:0,depth:0}),[i]),B=(0,s.useMemo)(()=>{let e=new o.LoY;e.setAttribute("value",new o.baL(j,1));let n=L*A*X;return e.setDrawRange(0,n),e},[j]),V=(0,s.useMemo)(()=>new o.BKk({glslVersion:o.Wdf,uniforms:{pointSize:{value:x},cmap:{value:r},cOffset:{value:y},cScale:{value:p},valueRange:{value:new o.I9Y(S[0],S[1])},scalePoints:{value:d},scaleIntensity:{value:m},startIDs:{value:D},stride:{value:M},showTransect:{value:z},dimWidth:{value:P},timeScale:{value:h},animateProg:{value:v},shape:{value:new o.Pq0(L,A,X)},flatBounds:{value:new o.IUQ(_[0],_[1],k[0],k[1])},vertBounds:{value:new o.I9Y(b[0],b[1])}},vertexShader:"attribute float value;\r\n\r\nout float vValue;\r\n\r\nflat out int highlight;\r\n\r\nuniform float pointSize;\r\nuniform bool scalePoints;\r\nuniform float scaleIntensity;\r\nuniform vec2 valueRange;\r\nuniform int[10] startIDs;\r\nuniform int stride;\r\nuniform int dimWidth;\r\nuniform bool showTransect;\r\nuniform float timeScale;\r\nuniform float animateProg;\r\nuniform vec4 flatBounds;\r\nuniform vec2 vertBounds;\r\nuniform vec3 shape;\r\n\r\nbool isValidPoint(){\r\n    for (int i = 0; i < 10; i++){\r\n        if (startIDs[i] == -1){\r\n            return false;\r\n        }\r\n        int rePos = gl_VertexID - startIDs[i];\r\n        bool isValid = rePos % stride == 0;\r\n        bool secondary = gl_VertexID < (startIDs[i] + dimWidth*stride) && gl_VertexID > startIDs[i];\r\n        isValid = isValid && secondary;\r\n        if (isValid){\r\n            return true;\r\n        }\r\n    }\r\n    return false;\r\n}\r\n\r\nvec3 computePosition(int vertexID) {\r\n    int depth = int(shape.x);\r\n    int height = int(shape.y);\r\n    int width = int(shape.z);\r\n\r\n    int sliceSize = width * height;\r\n\r\n    int z = vertexID / sliceSize;\r\n    int y = (vertexID % sliceSize) / width;\r\n    int x = vertexID % width;\r\n\r\n    float px = (float(x) - (float(width)/2.)) / 500.;\r\n    float py = (float(y) - (float(height)/2.)) / 500.;\r\n    float pz = (float(z) - (float(depth )/2.)) /500.;\r\n\r\n    return vec3(px * 2.0, py * 2.0, pz * 2.0);\r\n}\r\n\r\nvoid main() {\r\n    vValue = float(value)/255.;\r\n    vec3 scaledPos = computePosition(gl_VertexID);\r\n    float depthSize = float(shape.x)/500.;\r\n\r\n    scaledPos.z += depthSize;\r\n    scaledPos.z = mod(scaledPos.z + animateProg*depthSize*2., depthSize*2.);\r\n    scaledPos.z -= depthSize;\r\n\r\n    scaledPos.z *= timeScale;\r\n    gl_Position = projectionMatrix * modelViewMatrix * vec4(scaledPos, 1.0);\r\n\r\n    float pointScale = pointSize/gl_Position.w;\r\n    pointScale = scalePoints ? pointScale*pow(vValue,scaleIntensity) : pointScale;\r\n\r\n    bool isValid = isValidPoint();\r\n    highlight = isValid ? 1 : 0;\r\n    \r\n    if (value == 255. || (pointScale*gl_Position.w < 0.75 && scalePoints)){ //Hide points that are invisible or get too small when scalled\r\n        gl_Position = vec4(2.0, 2.0, 2.0, 1.0);\r\n    }\r\n\r\n    if (vValue < valueRange.x || vValue > valueRange.y){ //Hide points that are outside of value range\r\n        gl_Position = vec4(2.0, 2.0, 2.0, 1.0);\r\n    }\r\n\r\n    float scaleX = float(shape.z) / 500.0; //width scaling\r\n    float scaleY = float(shape.y) / 500.0; //height scaling\r\n    float scaleZ = float(shape.x) / 500.0; //depth scaling\r\n    \r\n    vec2 scaledXBounds = vec2(flatBounds.x, flatBounds.y) * scaleX;\r\n    vec2 scaledZBounds = vec2(flatBounds.z, flatBounds.w) * scaleZ * timeScale;\r\n    vec2 scaledYBounds = vec2(vertBounds.x, vertBounds.y) * scaleY;\r\n    \r\n    bool xCheck = scaledPos.x < scaledXBounds.x || scaledPos.x > scaledXBounds.y;\r\n    bool zCheck = scaledPos.z < scaledZBounds.x || scaledPos.z > scaledZBounds.y;\r\n    bool yCheck = scaledPos.y < scaledYBounds.x || scaledPos.y > scaledYBounds.y;\r\n\r\n    if (xCheck || zCheck || yCheck){ //Hide points that are clipped\r\n        gl_Position = vec4(2.0, 2.0, 2.0, 1.0);\r\n    }\r\n    \r\n    if (showTransect){\r\n        gl_PointSize = isValid ? max(pointScale*5., pointScale+80./gl_Position.w) : pointScale;\r\n    }\r\n    else{\r\n        gl_PointSize =  pointScale;\r\n    }\r\n\r\n}\r\n",fragmentShader:"out vec4 Color;\r\n\r\nin float vValue;\r\nflat in int highlight;\r\n\r\n\r\nuniform sampler2D cmap;\r\nuniform float cScale;\r\nuniform float cOffset;\r\nuniform bool showTransect;\r\n\r\nvoid main() {\r\n    float sampLoc = vValue == 1. ? vValue : (vValue - 0.5)*cScale + 0.5;\r\n    sampLoc = vValue == 1. ? vValue : min(sampLoc+cOffset,0.99);\r\n    vec4 color = texture(cmap, vec2(sampLoc, 0.5));\r\n    color.a = 1.;\r\n    Color = color;\r\n    if (showTransect){\r\n        Color = highlight == 1 ? color : color * vec4(vec3(0.4),1.);\r\n    }\r\n    else{\r\n        Color = color;\r\n    }\r\n}\r\n",depthWrite:!0,depthTest:!0,transparent:!1,blending:o.NTi,side:o.$EB}),[]);return(0,s.useEffect)(()=>{if(V){let e=V.uniforms;e.pointSize.value=x,e.cmap.value=r,e.cOffset.value=y,e.cScale.value=p,e.valueRange.value.set(S[0],S[1]),e.scalePoints.value=d,e.scaleIntensity.value=m,e.startIDs.value=D,e.stride.value=M,e.showTransect.value=z,e.dimWidth.value=P,e.timeScale.value=h,e.animateProg.value=v,e.flatBounds.value.set(_[0],_[1],k[0],k[1]),e.vertBounds.value.set(b[0],b[1])}},[x,r,y,p,S,d,m,D,M,z,v,h,_,b,k]),(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)("mesh",{scale:[1,u?-1:1,1],children:(0,a.jsx)("points",{geometry:B,material:V,frustumCulled:!1})}),(0,a.jsx)(g,{dimensions:{width:X,height:A,depth:L},ZarrDS:t,setters:{setPoints:I,setStride:O,setDimWidth:Y}})]})};var z=t(5606),h=t(3098);t(7181);var _=t(5798);t(1610);var b=t(9786),k=t(5922);function w(e){let{...n}=e;return(0,a.jsx)(b.bL,{"data-slot":"dropdown-menu",...n})}function I(e){let{...n}=e;return(0,a.jsx)(b.l9,{"data-slot":"dropdown-menu-trigger",...n})}function D(e){let{className:n,sideOffset:t=4,...i}=e;return(0,a.jsx)(b.ZL,{children:(0,a.jsx)(b.UC,{"data-slot":"dropdown-menu-content",sideOffset:t,className:(0,k.cn)("bg-popover text-popover-foreground data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 z-50 max-h-(--radix-dropdown-menu-content-available-height) min-w-[8rem] origin-(--radix-dropdown-menu-content-transform-origin) overflow-x-hidden overflow-y-auto rounded-md border p-1 shadow-md",n),...i})})}function C(e){let{...n}=e;return(0,a.jsx)(b.YJ,{"data-slot":"dropdown-menu-group",...n})}function M(e){let{className:n,inset:t,variant:i="default",...r}=e;return(0,a.jsx)(b.q7,{"data-slot":"dropdown-menu-item","data-inset":t,"data-variant":i,className:(0,k.cn)("focus:bg-accent focus:text-accent-foreground data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 dark:data-[variant=destructive]:focus:bg-destructive/20 data-[variant=destructive]:focus:text-destructive data-[variant=destructive]:*:[svg]:!text-destructive [&_svg:not([class*='text-'])]:text-muted-foreground relative flex cursor-default items-center gap-2 rounded-sm px-2 py-1.5 text-sm outline-hidden select-none data-[disabled]:pointer-events-none data-[disabled]:opacity-50 data-[inset]:pl-8 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",n),...r})}function O(e){let{className:n,...t}=e;return(0,a.jsx)(b.wv,{"data-slot":"dropdown-menu-separator",className:(0,k.cn)("bg-border -mx-1 my-1 h-px",n),...t})}var P=t(8797);let Y=s.memo(function(){let{showPoints:e,linePointSize:n,pointColor:t,useCustomPointColor:i,setLinePointSize:r,setPointColor:o,setUseCustomPointColor:l}=(0,c.Ws)((0,f.k)(e=>({showPoints:e.showPoints,linePointSize:e.linePointSize,pointColor:e.pointColor,useCustomPointColor:e.useCustomPointColor,setLinePointSize:e.setLinePointSize,setPointColor:e.setPointColor,setUseCustomPointColor:e.setUseCustomPointColor})));return e?(0,a.jsxs)(a.Fragment,{children:[(0,a.jsxs)("div",{className:"w-full flex items-center mb-2",children:[(0,a.jsx)("div",{className:"w-[40%]",children:(0,a.jsx)("b",{children:"Point Size"})}),(0,a.jsx)(P.A,{min:1,max:10,step:.1,value:[n],className:"flex-1 my-2",onValueChange:e=>r(e[0])})]}),(0,a.jsxs)("div",{className:"w-full flex items-center mb-2",children:[(0,a.jsx)("div",{className:"w-[40%]",children:(0,a.jsx)("b",{children:"Point Color"})}),i&&(0,a.jsx)("input",{type:"color",title:"Point Color",value:t,onChange:e=>o(e.target.value)}),(0,a.jsxs)(_.$,{className:"h-8 px-2 py-1",variant:"outline",onClick:()=>l(!i),children:["Use",i?" Browzarr Color":" Custom Color"]})]})]}):null}),j=s.memo(function(){let{lineWidth:e,lineColor:n,useLineColor:t,lineResolution:i,useCustomColor:r,setLineWidth:o,setLineColor:l,setUseLineColor:u,setLineResolution:s,setUseCustomColor:d}=(0,c.Ws)((0,f.k)(e=>({lineWidth:e.lineWidth,lineColor:e.lineColor,useLineColor:e.useLineColor,lineResolution:e.lineResolution,useCustomColor:e.useCustomColor,setLineWidth:e.setLineWidth,setLineColor:e.setLineColor,setUseLineColor:e.setUseLineColor,setLineResolution:e.setLineResolution,setUseCustomColor:e.setUseCustomColor})));return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsxs)("div",{className:"w-full flex items-center mb-2",children:[(0,a.jsx)("div",{className:"w-[40%]",children:(0,a.jsx)("b",{children:"Line Width"})}),(0,a.jsx)(P.A,{min:1,max:10,step:.2,value:[e],className:"flex-1 my-2",onValueChange:e=>o(e[0])})]}),(0,a.jsxs)("div",{className:"w-full flex items-center mb-2",children:[(0,a.jsx)("div",{className:"w-[40%]",children:(0,a.jsx)("b",{children:"Line Resolution"})}),(0,a.jsx)(P.A,{min:1,max:10,step:1,value:[i],className:"flex-1 my-2",onValueChange:e=>s(e[0])})]}),(0,a.jsxs)("div",{className:"w-full flex items-center",children:[(0,a.jsx)("div",{className:"w-[40%]",children:(0,a.jsx)("b",{children:"Line Color"})}),r&&(0,a.jsx)("input",{type:"color",title:"Line Color",value:n,onChange:e=>l(e.target.value)}),!r&&(0,a.jsxs)(_.$,{className:"h-8 px-2 py-5",variant:"outline",onClick:()=>u(!t),children:["Use ",(0,a.jsx)("br",{}),t?"Individual Color":"Plot Color"]}),(0,a.jsxs)(_.$,{className:"h-8 px-2 py-5",variant:"outline",onClick:()=>d(!r),children:["Use ",(0,a.jsx)("br",{}),r?"Browzarr Color":"Custom Color"]})]})]})}),X=s.memo(function(){let{showPoints:e,setShowPoints:n}=(0,c.Ws)((0,f.k)(e=>({showPoints:e.showPoints,setShowPoints:e.setShowPoints})));return(0,a.jsx)("div",{className:"plotline-options",children:(0,a.jsxs)(w,{children:[(0,a.jsx)(I,{asChild:!0,children:(0,a.jsx)(_.$,{className:"cursor-pointer",variant:"outline",children:"Line Options"})}),(0,a.jsxs)(D,{className:"w-80 items-center",align:"center",children:[(0,a.jsx)(C,{onClick:e=>e.preventDefault(),children:(0,a.jsx)(M,{children:(0,a.jsx)(_.$,{variant:"outline",onClick:t=>{t.preventDefault(),n(!e)},children:e?"Hide Points":"Show Points"})})}),(0,a.jsx)(Y,{}),(0,a.jsx)(O,{}),(0,a.jsx)(j,{})]})]})})});var A=t(5844);function L(e){let{pointID:n,pointLoc:t,showPointInfo:i,plotUnits:r}=e,{plotDim:o,dimArrays:l,dimNames:u,dimUnits:d,timeSeries:m}=(0,c.on)((0,f.k)(e=>({plotDim:e.plotDim,dimArrays:e.dimArrays,dimNames:e.dimNames,dimUnits:e.dimUnits,timeSeries:e.timeSeries}))),x=0,y=0;if(Object.entries(n).length>0&&Object.entries(m).length>0){let[e,t]=n;x=m[e].data[t],y=l[o][t]}let[S,g]=t,[v,z]=(0,s.useState)(!1);return(0,s.useEffect)(()=>{if(!i){let e=setTimeout(()=>{z(!1)},100);return()=>clearTimeout(e)}z(!0)},[i]),(0,a.jsx)(a.Fragment,{children:v&&(0,a.jsxs)("div",{className:"point-info",style:{left:"".concat(S,"px"),top:"".concat(g,"px")},children:["".concat(x.toFixed(2)).concat(r),(0,a.jsx)("br",{}),"".concat(u[o],": ").concat((0,p.SB)(y,d[o]),"       \n        ")]})})}function B(){let{coords:e,timeSeries:n,setDimCoords:t,setTimeSeries:i}=(0,c.on)((0,f.k)(e=>({coords:e.dimCoords,timeSeries:e.timeSeries,setDimCoords:e.setDimCoords,setTimeSeries:e.setTimeSeries}))),[r,o]=(0,s.useState)(!1),l=(0,s.useRef)([0,Math.round(.255*window.innerHeight)]),u=(0,s.useRef)([0,Math.round(.255*window.innerHeight)]),[d,m]=(0,s.useState)([0,Math.round(.255*window.innerHeight)]);function x(e){if(r){let n=l.current[0]-e.clientX,t=l.current[1]-e.clientY,i=Math.min(Math.max(u.current[0]-n,10),window.innerWidth-120),r=Math.max(u.current[1]+t,0);m([Math.min(i,window.innerWidth-100),Math.min(r,window.innerHeight-100)])}}function y(){o(!1)}return(0,s.useEffect)(()=>(r&&(document.addEventListener("mousemove",x),document.addEventListener("mouseup",y)),()=>{document.removeEventListener("mousemove",x),document.removeEventListener("mouseup",y)}),[r]),(0,a.jsx)(a.Fragment,{children:(0,a.jsx)("div",{className:"coord-container",onPointerDown:function(e){l.current=[e.clientX,e.clientY],u.current=[...d],o(!0)},onPointerMove:x,onPointerUp:()=>o(!1),style:{left:"".concat(d[0],"px"),bottom:"".concat(d[1],"px")},children:Object.keys(e).length>0&&Object.keys(e).reverse().map((r,o)=>(0,a.jsxs)("div",{className:"plot-coords",style:{background:"rgb(".concat(n[r].color,")"),justifyContent:"space-between"},children:[(0,a.jsx)("b",{children:"".concat(e[r].first.name,": ")}),"".concat((0,p.SB)(e[r].first.loc,e[r].first.units)),(0,a.jsx)("br",{}),(0,a.jsx)("b",{children:"".concat(e[r].second.name,": ")}),"".concat((0,p.SB)(e[r].second.loc,e[r].second.units)),(0,a.jsx)(A.Xr4,{onClick:()=>(function(r){let{[r]:a,...o}=e;t(o);let{[r]:l,...u}=n;i(u)})(r),color:"red",style:{cursor:"pointer",zIndex:3}})]},r))})})}function V(){let[e,n]=(0,s.useState)(["",0]),[t,i]=(0,s.useState)([0,0]),[r,o]=(0,s.useState)(!1),[l,u]=(0,s.useState)(Math.round(window.innerHeight-.25*window.innerHeight)),d=(0,c.on)(e=>e.metadata),f=d?d.units:"Default",[m,x]=(0,s.useState)(1),[p,y]=(0,s.useState)(1);(0,s.useEffect)(()=>{let e=()=>{let e=Math.round(window.innerHeight-.25*window.innerHeight);u(e),document.documentElement.style.setProperty("--plot-height","".concat(e,"px"))};return window.addEventListener("orientationchange",e),window.addEventListener("resize",e),()=>{window.removeEventListener("orientationchange",e),window.removeEventListener("resize",e)}},[]),(0,s.useEffect)(()=>{document.documentElement.style.setProperty("--plot-height","".concat(l,"px"))},[l]);let S=window.innerHeight-l>=10;return(0,a.jsxs)(a.Fragment,{children:[!S&&(0,a.jsx)(h.de,{onClick:()=>{u(window.innerHeight-60)}}),S&&(0,a.jsxs)("div",{className:"plot-canvas",children:[(0,a.jsx)(X,{}),r&&(0,a.jsx)(L,{pointID:e,pointLoc:t,showPointInfo:r,plotUnits:f}),(0,a.jsx)(h.A,{height:l,setHeight:u}),(0,a.jsx)(h.gj,{scale:m,setScale:x}),(0,a.jsx)(h.LC,{scale:p,setScale:y}),(0,a.jsxs)(z.Hl,{orthographic:!0,camera:{position:[0,0,100]},frameloop:"demand",children:[(0,a.jsx)(eq,{height:l,yScale:m,pointSetters:{setPointID:n,setPointLoc:i,setShowPointInfo:o},xScale:p}),(0,a.jsx)(E,{height:l,yScale:m,xScale:p})]}),(0,a.jsx)(B,{})]})]})}var N=t(8736),T=t(780);function E(e){let{colorTicks:n="grey",tickSize:t=4,fontSize:i=14,showGrid:r=!0,gridOpacity:o=.5,height:l,yScale:u=1,xScale:d=1}=e,{camera:x}=(0,m.C)(),[y,S]=(0,s.useState)({left:0,right:0,top:0,bottom:0}),{dimCoords:g,dimArrays:v,plotDim:z,valueScales:h}=(0,c.on)((0,f.k)(e=>({dimCoords:e.dimCoords,dimArrays:e.dimArrays,plotDim:e.plotDim,valueScales:e.valueScales}))),_=v[z],b=_.length,k=h.maxVal-h.minVal,w=(0,s.useMemo)(()=>{let e=["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"];if(_){if(_.every(e=>"bigint"==typeof e)){let n=Object.keys(g).length>0?Object.keys(g)[0]:null,t=n?(0,p.lt)(g[n].plot.units):1,i=[];for(let n=0;n<_.length;n++){let r=new Date(Number(_[n])*t),a="".concat(r.getDate().toString().padStart(2,"0")," ").concat(e[r.getMonth()],"\n").concat(r.getFullYear());i.push(a)}return i}return _.map(e=>String(e))}},[_,g]),I=(0,s.useMemo)(()=>{let e=window.innerWidth,n=window.innerHeight-l;return{left:-e/2+x.position.x,right:e/2+x.position.x,top:n/2+x.position.y,bottom:-n/2+x.position.y}},[]),[D,C]=(0,s.useState)(x.zoom),M=(0,s.useMemo)(()=>{let e=1/x.zoom;return{tickSize:t*e,fontSize:i/e,labelOffset:t*e}},[x.zoom,t,i]);(0,m.D)(()=>{x.zoom!==D&&C(x.zoom);let e=window.innerWidth/x.zoom,n=(window.innerHeight-l)/x.zoom,t={left:-e/2+x.position.x,right:e/2+x.position.x,top:n/2+x.position.y,bottom:-n/2+x.position.y};JSON.stringify(y)!=JSON.stringify(t)&&S(t)});let O=(0,s.useRef)(null),P=(0,s.useRef)(null);(0,s.useEffect)(()=>(O.current&&clearTimeout(O.current),P.current&&(O.current=setTimeout(()=>{P.current&&P.current.reset()},100)),()=>{null!==O.current&&clearTimeout(O.current)}),[l]);let Y=(y.top+y.bottom)/2,j=(y.left+y.right)/2;return(0,a.jsxs)("group",{children:[r&&(0,a.jsxs)(a.Fragment,{children:[Array.from({length:10},(e,t)=>{var i;if(0===t||9===t)return null;let r=+Math.round(y.left/1)+(Math.round(y.right/1)-Math.round(y.left/1))*(t/9),l=r/d/(I.right-I.left)+.5;return(0,a.jsxs)(s.Fragment,{children:[(0,a.jsx)("group",{position:[r,Y,0],children:(0,a.jsxs)("line",{children:[(0,a.jsx)("bufferGeometry",{children:(0,a.jsx)("float32BufferAttribute",{attach:"attributes-position",args:[new Float32Array([0,y.top-Y,0,0,y.bottom-Y,0]),3]})}),(0,a.jsx)("lineDashedMaterial",{color:n,opacity:o,transparent:!0,dashSize:.5,gapSize:.5})]},"vgrid-".concat(t))}),(0,a.jsxs)("group",{position:[r,y.top,0],children:[(0,a.jsxs)("line",{children:[(0,a.jsx)("bufferGeometry",{children:(0,a.jsx)("float32BufferAttribute",{attach:"attributes-position",args:[new Float32Array([0,0,0,0,-M.tickSize,0]),3]})}),(0,a.jsx)("lineBasicMaterial",{color:n})]}),0!==t&&9!==t&&(0,a.jsx)(N.E,{position:[0,M.tickSize/4-M.labelOffset,0],fontSize:M.fontSize/D**2,color:n,anchorX:"center",anchorY:"top",children:null!=(i=null==w?void 0:w[Math.round(l*b-.5)])?i:""})]},"top-tick-".concat(t))]},"vert-group-".concat(t))}),Array.from({length:8},(e,t)=>{if(0===t||7===t)return null;let i=y.bottom+(y.top-y.bottom)*(t/7),r=i/u/(y.top-y.bottom)/D+.5;return(0,a.jsxs)(s.Fragment,{children:[(0,a.jsx)("group",{position:[j,i,0],children:(0,a.jsxs)("line",{children:[(0,a.jsx)("bufferGeometry",{children:(0,a.jsx)("float32BufferAttribute",{attach:"attributes-position",args:[new Float32Array([y.left-j,0,0,y.right-j,0,0]),3]})}),(0,a.jsx)("lineDashedMaterial",{color:n,opacity:o,transparent:!0,dashSize:0,gapSize:.5,linewidth:1})]})},"hgrid-".concat(t)),(0,a.jsxs)("group",{position:[y.right,i,0],children:[(0,a.jsxs)("line",{children:[(0,a.jsx)("bufferGeometry",{children:(0,a.jsx)("float32BufferAttribute",{attach:"attributes-position",args:[new Float32Array([0,0,0,-M.tickSize,0,0]),3]})}),(0,a.jsx)("lineBasicMaterial",{color:n})]}),0!==t&&7!==t&&(0,a.jsx)(N.E,{position:[-M.tickSize-M.labelOffset,0,0],fontSize:M.fontSize/D**2,color:n,anchorX:"right",anchorY:"middle",children:(h.minVal+r*k).toFixed(1)})]},"right-tick-".concat(t))]},"vert-group-".concat(t))})]}),(0,a.jsx)(T.N,{ref:P,enableRotate:!1,enablePan:!0,enableZoom:!0,zoomSpeed:.85,maxDistance:500,maxZoom:20,minZoom:.5})]})}var R=t(1785);t(3583);let U=e=>{let{loc:n,show:t,info:i}=e,{dimNames:r,dimUnits:o}=(0,c.on)((0,f.k)(e=>({dimNames:e.dimNames,dimUnits:e.dimUnits}))),l=(0,c.r2)(e=>e.axis),u=(0,s.useMemo)(()=>r.length<3?[r[0],r[1]]:r.filter((e,n)=>n!=l),[r,l]),d=(0,s.useMemo)(()=>r.length<3?[o[0],o[1]]:o.filter((e,n)=>n!=l),[o,l]);return(0,a.jsxs)("div",{className:"analysis-overlay",style:{left:"".concat(n[0]+10,"px"),top:"".concat(n[1]+10,"px"),display:t?"":"none"},children:["".concat(u[0],": ").concat(t&&(0,p.SB)(i[0],d[0])),(0,a.jsx)("br",{}),"".concat(u[1],": ").concat(t&&(0,p.SB)(i[1],d[1])),(0,a.jsx)("br",{}),"Value: ".concat(Math.round(100*i[2])/100)]})};var G=t(9301);let F="\n    struct Params {\n        zStride: u32,\n        yStride: u32,\n        xStride: u32,\n        xSize: u32,\n        ySize: u32,\n        reduceDim: u32,\n        dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f32>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n",Z="\n    ".concat(F,"\n        var sum: f32 = 0.0;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                sum += inputData[inputIndex];\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                sum += inputData[inputIndex];\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                sum += inputData[inputIndex];\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = sum / f32(dimLength);\n    }\n"),q="\n    ".concat(F,"\n        var min: f32 = 1e12;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let newMin = inputData[inputIndex];\n                if (newMin < min) {\n                    min = newMin;\n                }\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let newMin = inputData[inputIndex];\n                if (newMin < min) {\n                    min = newMin;\n                }\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let newMin = inputData[inputIndex];\n                if (newMin < min) {\n                    min = newMin;\n                }\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = min;\n    }\n"),W="\n    ".concat(F,"\n        \n        var max: f32 = -1e12;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let newMax = inputData[inputIndex];\n                if (newMax > max) {\n                    max = newMax;\n                }\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let newMax = inputData[inputIndex];\n                if (newMax > max) {\n                    max = newMax;\n                }\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let newMax = inputData[inputIndex];\n                if (newMax > max) {\n                    max = newMax;\n                }\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = max;\n    }\n"),H="\n    ".concat(F,"\n        var sum: f32 = 0.0;\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                sum += inputData[inputIndex];\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                sum += inputData[inputIndex];\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                sum += inputData[inputIndex];\n            }\n        }\n        \n        let mean: f32 = sum / f32(dimLength);\n\n        var squaredDiffSum: f32 = 0.0;\n\n        // Iterate along the dimension again\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let diff: f32 = mean - inputData[inputIndex];\n                squaredDiffSum += diff*diff;\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let diff: f32 = mean - inputData[inputIndex];\n                squaredDiffSum += diff*diff;\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let diff: f32 = mean - inputData[inputIndex];\n                squaredDiffSum += diff*diff;\n            }\n        }\n\n        let stDev: f32 = sqrt(squaredDiffSum / f32(dimLength));\n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = stDev;\n    }\n"),Q="\n    ".concat(F,"\n        \n        var accum: f32 = 0;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                accum += inputData[inputIndex];\n                \n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                accum += inputData[inputIndex];\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                accum += inputData[inputIndex];\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = accum;\n    }\n"),$="\n    ".concat(F,"\n        let meanY: f32 = f32(dimLength)/2;\n        var sum: f32 = 0.0;\n\n\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                sum += inputData[inputIndex];\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                sum += inputData[inputIndex];\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                sum += inputData[inputIndex];\n            }\n        }\n        \n        let meanX: f32 = sum / f32(dimLength);\n        var numSum: f32 = 0;\n        var denomSum: f32 = 0;\n\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let xi: f32 = inputData[inputIndex];\n                numSum += (xi - meanX)*(f32(z) - meanY);\n                denomSum += (f32(z) - meanY)*(f32(z) - meanY);\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let xi: f32 = inputData[inputIndex];\n                numSum += (xi - meanX)*(f32(y) - meanY);\n                denomSum += (f32(y) - meanY)*(f32(y) - meanY);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let xi: f32 = inputData[inputIndex];\n                numSum += (xi - meanX)*(f32(x) - meanY);\n                denomSum += (f32(x) - meanY)*(f32(x) - meanY);\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = numSum/denomSum;\n    }\n"),J="\n    struct Params {\n    zStride: u32,\n    yStride: u32,\n    xStride: u32,\n    xSize: u32,\n    ySize: u32,\n    reduceDim: u32,\n    dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f32>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f32>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n\n        var ySum: f32 = 0;\n        var xSum: f32 = 0.0;\n\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { \n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                xSum += firstData[inputIndex];\n                ySum += secondData[inputIndex];\n            }\n        } else if (reduceDim == 1u) { \n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                xSum += firstData[inputIndex];\n                ySum += secondData[inputIndex];\n            }\n        } else { \n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                xSum += firstData[inputIndex];\n                ySum += secondData[inputIndex];\n            }\n        }\n        \n        let xMean: f32 = xSum / f32(dimLength);\n        let yMean: f32 = ySum / f32(dimLength);\n        var numSum: f32 = 0;\n        var denomSum: f32 = 0;\n\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let xi: f32 = firstData[inputIndex];\n                let yi: f32 = secondData[inputIndex];\n                numSum += (xi - xMean)*(f32(yi) - yMean);\n                denomSum += (f32(yi) - yMean)*(f32(yi) - yMean);\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let xi: f32 = firstData[inputIndex];\n                let yi: f32 = secondData[inputIndex];\n                numSum += (xi - xMean)*(f32(yi) - yMean);\n                denomSum += (f32(yi) - yMean)*(f32(yi) - yMean);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let xi: f32 = firstData[inputIndex];\n                let yi: f32 = secondData[inputIndex];\n                numSum += (xi - xMean)*(f32(yi) - yMean);\n                denomSum += (f32(yi) - yMean)*(f32(yi) - yMean);\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = numSum/denomSum;\n    }\n",K="\n    struct Params {\n    zStride: u32,\n    yStride: u32,\n    xStride: u32,\n    xSize: u32,\n    ySize: u32,\n    reduceDim: u32,\n    dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f32>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f32>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n        var ySum: f32 = 0;\n        var xSum: f32 = 0.0;\n        var numSum: f32 = 0;\n\n       // Calculate base coordinate and stride for the dimension we're iterating over\n        var baseCoord: u32;\n        var iterStride: u32;\n\n        if (reduceDim == 0u) {\n            baseCoord = outX * xStride + outY * yStride;\n            iterStride = zStride;\n        } else if (reduceDim == 1u) {\n            baseCoord = outX * xStride + outY * zStride;\n            iterStride = yStride;\n        } else {\n            baseCoord = outX * yStride + outY * zStride;\n            iterStride = xStride;\n        }\n\n        // Single pass: calculate sums, means, and covariance\n        for (var i: u32 = 0u; i < dimLength; i++) {\n            let inputIndex = baseCoord + (i * iterStride);\n            let xi: f32 = firstData[inputIndex];\n            let yi: f32 = secondData[inputIndex];\n            xSum += xi;\n            ySum += yi;\n        }\n\n        let xMean: f32 = xSum / f32(dimLength);\n        let yMean: f32 = ySum / f32(dimLength);\n\n        // Second pass for covariance calculation\n        for (var i: u32 = 0u; i < dimLength; i++) {\n            let inputIndex = baseCoord + (i * iterStride);\n            let xi: f32 = firstData[inputIndex];\n            let yi: f32 = secondData[inputIndex];\n            numSum += (xi - xMean) * (yi - yMean);\n        }\n\n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = numSum / (f32(dimLength) - 1);\n    }\n",ee="\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f32>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n",en="\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        xSize: u32,\n        ySize: u32,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f32>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>,) {\n        let xStride = params.xStride; \n        let yStride = params.yStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let kernelSize = params.kernelSize;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n\n        if (outX >= xSize|| outY >= ySize) {\n            return;\n        }\n\n        let globalIdx = outY * xSize + outX;\n        let thisVal = inputData[globalIdx];\n        let isNaN: bool = thisVal != thisVal;\n        if (isNaN){\n            outputData[globalIdx] = thisVal;\n            return;\n        }   \n\n        let xy_radius: i32 = i32(kernelSize/2u);\n\n",et="\n        ".concat(ee,"    \n        var sum: f32 = 0.0;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        sum += inputData[u32(newIdx)];\n                        count ++;\n                    }\n                }\n            }\n        }\n        \n        outputData[globalIdx] = sum / f32(count);\n    }\n"),ei="\n    ".concat(ee,"  \n        var minVal: f32 = 1e12;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let sampledVal = inputData[u32(newIdx)];\n                        if (sampledVal < minVal){\n                            minVal = sampledVal;\n                        }\n                    }\n                }\n            }\n        }\n        \n        outputData[globalIdx] = minVal;\n    }\n"),er="\n    ".concat(ee,"  \n\n        var maxVal: f32 = -1e12;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let sampledVal = inputData[u32(newIdx)];\n                        if (sampledVal > maxVal){\n                            maxVal = sampledVal;\n                        }\n                    }\n                }\n            }\n        }\n        \n        outputData[globalIdx] = maxVal;\n    }\n"),ea="\n    ".concat(ee,"  \n        var sum: f32 = 0.0;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        sum += inputData[u32(newIdx)];\n                        count ++;\n                    }\n                }\n            }\n        }\n        \n        let mean: f32 = sum / f32(count);\n\n        var squaredDiffSum: f32 = 0.0;\n\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        let diff: f32 = mean - inputData[u32(newIdx)];\n                        squaredDiffSum += diff*diff;\n                    }\n                }\n            }\n        }\n\n        let stDev: f32 = sqrt(squaredDiffSum / f32(count));\n\n        outputData[globalIdx] = stDev;\n    }\n"),eo="\n        ".concat(en,"    \n        var sum: f32 = 0;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = inputData[u32(newIdx)];\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    sum += newVal;\n                    count ++;\n                }\n            }\n        }\n        outputData[globalIdx] = sum / f32(count);\n    }\n"),el="\n    ".concat(en,"   \n        var minVal: f32 = 1e12;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = inputData[u32(newIdx)];\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    if (newVal < minVal){\n                        minVal = newVal;\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = minVal;\n    }\n"),eu="\n    ".concat(en,"  \n        var maxVal: f32 = -1e12;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = inputData[u32(newIdx)];\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    if (newVal > maxVal){\n                        maxVal = newVal;\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = maxVal;\n    }\n"),es="\n     ".concat(en,"  \n        var sum: f32 = 1e12;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = inputData[u32(newIdx)];\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    sum += newVal;\n                    count ++;\n                }\n            }\n        }\n        \n        let mean: f32 = sum / f32(count);\n\n        var squaredDiffSum: f32 = 0.0;\n\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = inputData[u32(newIdx)];\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    let diff: f32 = mean - newVal;\n                    squaredDiffSum += diff*diff;\n                }\n            }\n        }\n        let stDev: f32 = sqrt(squaredDiffSum / f32(count));\n\n        outputData[globalIdx] = stDev;\n    }\n"),ed="\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f32>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f32>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n\n        var xSum: f32 = 0.0;\n        var xxSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n        var yySum: f32 = 0.0;\n        var xySum: f32 = 0.0;\n\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        let xI = firstData[newIdx];\n                        let yI = secondData[newIdx];\n                        xSum += xI;\n                        xxSum += xI * xI;\n                        ySum += yI;\n                        yySum += yI * yI;\n                        xySum += xI * yI;\n                        count ++;\n                    }\n                }\n            }\n        }\n\n        let N: f32 = f32(count);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        let varX = (xxSum / N) - (meanX * meanX);\n        let varY = (yySum / N) - (meanY * meanY);\n        let covXY = (xySum / N) - (meanX * meanY);\n        let sigmaX = sqrt(max(0.0, varX));\n        let sigmaY = sqrt(max(0.0, varY));\n        let epsilon = 1e-6;\n        let denominator = sigmaX * sigmaY + epsilon;\n        let correlation = covXY / denominator;\n\n        outputData[globalIdx] = correlation;\n    }\n",ec="\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f32>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f32>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n\n        var xSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n        var numSum: f32 = 0.0;\n\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = firstData[newIdx];\n                        let yI = secondData[newIdx];\n                        xSum += xI;    \n                        ySum += yI;\n                        count ++;\n                    }\n                }\n            }\n        }\n\n        let N: f32 = f32(count);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        \n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = firstData[newIdx];\n                        let yI = secondData[newIdx];\n                        numSum += (xI - meanX) * (yI - meanY);\n                        count ++;\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = numSum/(N-1);;\n    }\n",ef="\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f32>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f32>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n\n        var xSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = firstData[newIdx];\n                        let yI = secondData[newIdx];\n                        xSum += xI;    \n                        ySum += yI;\n                        count ++;\n                    }\n                }\n            }\n        }\n\n\n        let N: f32 = f32(count);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        var numSum: f32 = 0;\n        var denomSum: f32 = 0;\n        \n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = firstData[newIdx];\n                        let yI = secondData[newIdx];\n                        numSum += (xI - meanX)*(f32(yI) - meanY);\n                        denomSum += (f32(yI) - meanY)*(f32(yI) - meanY);\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = numSum/denomSum;;\n    }\n",em="\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        reduceDim: u32,\n        reverse: u32,\n        workGroups: vec3<u32>,\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f32>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let reverse = params.reverse;\n        let workGroups = params.workGroups;\n        let reduceDim = params.reduceDim;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n        let totalSize: u32 = xSize * ySize * zSize;\n        var baseIdx = outZ * zStride + outY * yStride + outX * xStride;\n        var accum: f32 = 0;\n\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // CUMSUM along Z\n            if (reverse == u32(1)){\n                baseIdx = (zSize - outZ - 1) * zStride + outY * yStride + outX * xStride;\n            }\n            for (var z: u32 = 0u; z < outZ; z++) {\n                var newZ: u32 = z;\n                if (reverse == u32(1)){\n                    newZ = zSize - z - 1;\n                }\n                let idx = newZ * zStride + outY * yStride + outX * xStride;\n                accum += inputData[idx];\n            }\n\n        } else if (reduceDim == 1u) { // CUMSUM along Y\n            if (reverse == u32(1)){\n                baseIdx = outZ * zStride + (ySize - outY - 1)* yStride + outX * xStride;\n            }\n            for (var y: u32 = 0u; y < outY; y++) {\n                var newY: u32 = y;\n                if (reverse == u32(1)){\n                    newY = ySize - y - 1;\n                }\n                let idx = outZ * zStride + newY * yStride + outX * xStride;\n                accum += inputData[idx];\n            }\n        } else { // CUMSUM along X\n             if (reverse == u32(1)){\n                baseIdx = outZ * zStride + outY* yStride + (xSize - outX - 1) * xStride;\n            }\n            for (var x: u32 = 0u; x < outX; x++) {\n                var newX: u32 = x;\n                if (reverse == u32(1)){\n                    newX = xSize - x - 1;\n                }\n                let idx = outZ * zStride + outY * yStride + newX * xStride;\n                accum += inputData[idx];\n            }\n        }\n            outputData[baseIdx] = accum;\n    }\n",ex="\n    struct Params {\n        zStride: u32,\n        yStride: u32,\n        xStride: u32,\n        xSize: u32,\n        ySize: u32,\n        reduceDim: u32,\n        dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f32>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f32>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n\n        var xSum: f32 = 0.0;\n        var xxSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n        var yySum: f32 = 0.0;\n        var xySum: f32 = 0.0;\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let xI = firstData[inputIndex];\n                let yI = secondData[inputIndex];\n                xSum += xI;\n                xxSum += xI * xI;\n                ySum += yI;\n                yySum += yI * yI;\n                xySum += xI * yI;\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let xI = firstData[inputIndex];\n                let yI = secondData[inputIndex];\n                xSum += xI;\n                xxSum += xI * xI;\n                ySum += yI;\n                yySum += yI * yI;\n                xySum += xI * yI;\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let xI = firstData[inputIndex];\n                let yI = secondData[inputIndex];\n                xSum += xI;\n                xxSum += xI * xI;\n                ySum += yI;\n                yySum += yI * yI;\n                xySum += xI * yI;\n            }\n        }\n\n        let N: f32 = f32(dimLength);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        let varX = (xxSum / N) - (meanX * meanX);\n        let varY = (yySum / N) - (meanY * meanY);\n        let covXY = (xySum / N) - (meanX * meanY);\n        let sigmaX = sqrt(max(0.0, varX));\n        let sigmaY = sqrt(max(0.0, varY));\n        let epsilon = 1e-6;\n        let denominator = sigmaX * sigmaY + epsilon;\n        let correlation = covXY / denominator;\n\n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = correlation;\n    }\n",ep="\n    enable f16;\n    struct Params {\n        zStride: u32,\n        yStride: u32,\n        xStride: u32,\n        xSize: u32,\n        ySize: u32,\n        reduceDim: u32,\n        dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f16>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n",ey="\n    ".concat(ep,"\n        var sum: f32 = 0.0;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(sum / f32(dimLength));\n    }\n"),eS="\n    ".concat(ep,"\n        var min: f32 = 1e12;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let newMin = f32(inputData[inputIndex]);\n                if (newMin < min) {\n                    min = newMin;\n                }\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let newMin = f32(inputData[inputIndex]);\n                if (newMin < min) {\n                    min = newMin;\n                }\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let newMin = f32(inputData[inputIndex]);\n                if (newMin < min) {\n                    min = newMin;\n                }\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(min);\n    }\n"),eg="\n    ".concat(ep,"\n        \n        var max: f32 = -1e12;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let newMax = f32(inputData[inputIndex]);\n                if (newMax > max) {\n                    max = newMax;\n                }\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let newMax = f32(inputData[inputIndex]);\n                if (newMax > max) {\n                    max = newMax;\n                }\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let newMax = f32(inputData[inputIndex]);\n                if (newMax > max) {\n                    max = newMax;\n                }\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(max);\n    }\n"),ev="\n    ".concat(ep,"\n        var sum: f32 = 0.0;\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        }\n        \n        let mean: f32 = sum / f32(dimLength);\n\n        var squaredDiffSum: f32 = 0.0;\n\n        // Iterate along the dimension again\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let diff: f32 = mean - f32(inputData[inputIndex]);\n                squaredDiffSum += diff*diff;\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let diff: f32 = mean - f32(inputData[inputIndex]);\n                squaredDiffSum += diff*diff;\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let diff: f32 = mean - f32(inputData[inputIndex]);\n                squaredDiffSum += diff*diff;\n            }\n        }\n\n        let stDev: f32 = sqrt(squaredDiffSum / f32(dimLength));\n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(stDev);\n    }\n"),ez="\n    enable f16;\n    struct Params {\n        zStride: u32,\n        yStride: u32,\n        xStride: u32,\n        xSize: u32,\n        ySize: u32,\n        reduceDim: u32,\n        dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f16>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n        \n        var accum: f32 = 0;\n        \n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                accum += f32(inputData[inputIndex]);\n                \n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                accum += f32(inputData[inputIndex]);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                accum += f32(inputData[inputIndex]);\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = accum;\n    }\n",eh="\n    ".concat(ep,"\n        let meanY: f32 = f32(dimLength)/2;\n        var sum: f32 = 0.0;\n\n\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                sum += f32(inputData[inputIndex]);\n            }\n        }\n        \n        let meanX: f32 = sum / f32(dimLength);\n        var numSum: f32 = 0;\n        var denomSum: f32 = 0;\n\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let xi: f32 = f32(inputData[inputIndex]);\n                numSum += (xi - meanX)*(f32(z) - meanY);\n                denomSum += (f32(z) - meanY)*(f32(z) - meanY);\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let xi: f32 = f32(inputData[inputIndex]);\n                numSum += (xi - meanX)*(f32(y) - meanY);\n                denomSum += (f32(y) - meanY)*(f32(y) - meanY);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let xi: f32 = f32(inputData[inputIndex]);\n                numSum += (xi - meanX)*(f32(x) - meanY);\n                denomSum += (f32(x) - meanY)*(f32(x) - meanY);\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(numSum/denomSum);\n    }\n"),e_="\n    enable f16;\n    struct Params {\n    zStride: u32,\n    yStride: u32,\n    xStride: u32,\n    xSize: u32,\n    ySize: u32,\n    reduceDim: u32,\n    dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f16>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f16>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n\n        var ySum: f32 = 0;\n        var xSum: f32 = 0.0;\n\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { \n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                xSum += f32(firstData[inputIndex]);\n                ySum += f32(secondData[inputIndex]);\n            }\n        } else if (reduceDim == 1u) { \n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                xSum += f32(firstData[inputIndex]);\n                ySum += f32(secondData[inputIndex]);\n            }\n        } else { \n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                xSum += f32(firstData[inputIndex]);\n                ySum += f32(secondData[inputIndex]);\n            }\n        }\n        \n        let xMean: f32 = xSum / f32(dimLength);\n        let yMean: f32 = ySum / f32(dimLength);\n        var numSum: f32 = 0;\n        var denomSum: f32 = 0;\n\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let xi: f32 = f32(firstData[inputIndex]);\n                let yi: f32 = f32(secondData[inputIndex]);\n                numSum += (xi - xMean)*(f32(yi) - yMean);\n                denomSum += (f32(yi) - yMean)*(f32(yi) - yMean);\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let xi: f32 = f32(firstData[inputIndex]);\n                let yi: f32 = f32(secondData[inputIndex]);\n                numSum += (xi - xMean)*(f32(yi) - yMean);\n                denomSum += (f32(yi) - yMean)*(f32(yi) - yMean);\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let xi: f32 = f32(firstData[inputIndex]);\n                let yi: f32 = f32(secondData[inputIndex]);\n                numSum += (xi - xMean)*(f32(yi) - yMean);\n                denomSum += (f32(yi) - yMean)*(f32(yi) - yMean);\n            }\n        }\n        \n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(numSum/(denomSum+1e-4));\n    }\n",eb="\n    enable f16;\n    struct Params {\n    zStride: u32,\n    yStride: u32,\n    xStride: u32,\n    xSize: u32,\n    ySize: u32,\n    reduceDim: u32,\n    dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f16>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f16>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n        var ySum: f32 = 0;\n        var xSum: f32 = 0.0;\n        var numSum: f32 = 0;\n\n       // Calculate base coordinate and stride for the dimension we're iterating over\n        var baseCoord: u32;\n        var iterStride: u32;\n\n        if (reduceDim == 0u) {\n            baseCoord = outX * xStride + outY * yStride;\n            iterStride = zStride;\n        } else if (reduceDim == 1u) {\n            baseCoord = outX * xStride + outY * zStride;\n            iterStride = yStride;\n        } else {\n            baseCoord = outX * yStride + outY * zStride;\n            iterStride = xStride;\n        }\n\n        // Single pass: calculate sums, means, and covariance\n        for (var i: u32 = 0u; i < dimLength; i++) {\n            let inputIndex = baseCoord + (i * iterStride);\n            let xi: f32 = f32(firstData[inputIndex]);\n            let yi: f32 = f32(secondData[inputIndex]);\n            xSum += xi;\n            ySum += yi;\n        }\n\n        let xMean: f32 = xSum / f32(dimLength);\n        let yMean: f32 = ySum / f32(dimLength);\n\n        // Second pass for covariance calculation\n        for (var i: u32 = 0u; i < dimLength; i++) {\n            let inputIndex = baseCoord + (i * iterStride);\n            let xi: f32 = f32(firstData[inputIndex]);\n            let yi: f32 = f32(secondData[inputIndex]);\n            numSum += (xi - xMean) * (yi - yMean);\n        }\n\n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(numSum / (f32(dimLength) - 1));\n    }\n",ek="\n    enable f16;\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f16>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(2) var<uniform> params: Params;\n    \n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n",ew="\n    enable f16;\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        xSize: u32,\n        ySize: u32,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f16>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>,) {\n        let xStride = params.xStride; \n        let yStride = params.yStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let kernelSize = params.kernelSize;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n\n        if (outX >= xSize|| outY >= ySize) {\n            return;\n        }\n\n        let globalIdx = outY * xSize + outX;\n        let thisVal = inputData[globalIdx];\n        let isNaN: bool = thisVal != thisVal;\n        if (isNaN){\n            outputData[globalIdx] = thisVal;\n            return;\n        }   \n\n        let xy_radius: i32 = i32(kernelSize/2u);\n\n",eI="\n        ".concat(ek,"    \n        var sum: f32 = 0.0;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        sum += f32(inputData[u32(newIdx)]);\n                        count ++;\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = f16(sum / f32(count));\n    }\n"),eD="\n    ".concat(ek,"  \n        var minVal: f32 = 1e12;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let sampledVal = f32(inputData[u32(newIdx)]);\n                        if (sampledVal < minVal){\n                            minVal = sampledVal;\n                        }\n                    }\n                }\n            }\n        }\n        \n        outputData[globalIdx] = f16(minVal);\n    }\n"),eC="\n    ".concat(ek,"  \n\n        var maxVal: f32 = -1e12;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let sampledVal = f32(inputData[u32(newIdx)]);\n                        if (sampledVal > maxVal){\n                            maxVal = sampledVal;\n                        }\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = f16(maxVal);\n    }\n"),eM="\n    ".concat(ek,"  \n        var sum: f32 = 0.0;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        sum += f32(inputData[u32(newIdx)]);\n                        count ++;\n                    }\n                }\n            }\n        }\n        \n        let mean: f32 = sum / f32(count);\n\n        var squaredDiffSum: f32 = 0.0;\n\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        let diff: f32 = mean - f32(inputData[u32(newIdx)]);\n                        squaredDiffSum += diff*diff;\n                    }\n                }\n            }\n        }\n\n        let stDev: f32 = sqrt(squaredDiffSum / f32(count));\n\n        outputData[globalIdx] = f16(stDev);\n    }\n"),eO="\n        ".concat(ew,"    \n        var sum: f32 = 0;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = f32(inputData[u32(newIdx)]);\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    sum += newVal;\n                    count ++;\n                }\n            }\n        }\n        outputData[globalIdx] = f16(sum / f32(count));\n    }\n"),eP="\n    ".concat(ew,"   \n        var minVal: f32 = 1e12;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = f32(inputData[u32(newIdx)]);\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    if (newVal < minVal){\n                        minVal = newVal;\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = f16(minVal);\n    }\n"),eY="\n    ".concat(ew,"  \n        var maxVal: f32 = -1e12;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = f32(inputData[u32(newIdx)]);\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    if (newVal > maxVal){\n                        maxVal = newVal;\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = f16(maxVal);\n    }\n"),ej="\n     ".concat(ew,"  \n        var sum: f32 = 0.;\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = f32(inputData[u32(newIdx)]);\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    sum += newVal;\n                    count ++;\n                }\n            }\n        }\n        \n        let mean: f32 = sum / f32(count);\n\n        var squaredDiffSum: f32 = 0.0;\n\n        for (var kx: i32 = -xy_radius; kx <= xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius; ky <= xy_radius; ky++) {\n                let in_coord = vec2<i32>(i32(global_id.x), i32(global_id.y)) + vec2<i32>(kx, ky);\n                if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                    in_coord.y >= 0 && in_coord.y < i32(ySize)) { //Ensure the sampled point is within 3D dataspace\n                    let xOffset = kx * i32(xStride);\n                    let yOffset = ky * i32(yStride);\n                    let newIdx = i32(globalIdx) + xOffset + yOffset;\n                    let newVal = f32(inputData[u32(newIdx)]);\n                    if (newVal != newVal){ //This only evaluates if newVal is NaN\n                        continue;\n                    }\n                    let diff: f32 = mean - newVal;\n                    squaredDiffSum += diff*diff;\n                }\n            }\n        }\n        let stDev: f32 = sqrt(squaredDiffSum / f32(count));\n\n        outputData[globalIdx] = f16(stDev);\n    }\n"),eX="\n    enable f16;\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f16>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f16>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n\n        var xSum: f32 = 0.0;\n        var xxSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n        var yySum: f32 = 0.0;\n        var xySum: f32 = 0.0;\n\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { //Ensure the sampled point is within 3D dataspace\n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n\n                        let xI = f32(firstData[newIdx]);\n                        let yI = f32(secondData[newIdx]);\n                        xSum += xI;\n                        xxSum += xI * xI;\n                        ySum += yI;\n                        yySum += yI * yI;\n                        xySum += xI * yI;\n                        count ++;\n                    }\n                }\n            }\n        }\n\n        let N: f32 = f32(count);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        let varX = (xxSum / N) - (meanX * meanX);\n        let varY = (yySum / N) - (meanY * meanY);\n        let covXY = (xySum / N) - (meanX * meanY);\n        let sigmaX = sqrt(max(0.0, varX));\n        let sigmaY = sqrt(max(0.0, varY));\n        let epsilon = 1e-6;\n        let denominator = sigmaX * sigmaY + epsilon;\n        let correlation = covXY / denominator;\n\n        outputData[globalIdx] = f16(correlation);\n    }\n",eA="\n    enable f16;\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f16>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f16>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n\n        var xSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n        var numSum: f32 = 0.0;\n\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = f32(firstData[newIdx]);\n                        let yI = f32(secondData[newIdx]);\n                        xSum += xI;    \n                        ySum += yI;\n                        count ++;\n                    }\n                }\n            }\n        }\n\n        let N: f32 = f32(count);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        \n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = f32(firstData[newIdx]);\n                        let yI = f32(secondData[newIdx]);\n                        numSum += (xI - meanX) * (yI - meanY);\n                        count ++;\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = f16(numSum/(N-1));\n    }\n",eL="\n    enable f16;\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        workGroups: vec3<u32>,\n        kernelSize: u32,\n        kernelDepth: u32\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f16>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f16>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let workGroups = params.workGroups;\n        let kernelSize = params.kernelSize;\n        let kernelDepth = params.kernelDepth;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n\n        let total_threads_per_slice = workGroups.x * workGroups.y * 16;\n        let globalIdx = global_id.z * total_threads_per_slice + \n                        global_id.y * (workGroups.x * 4) + \n                        global_id.x;\n\n        let xy_radius: i32 = i32(kernelSize/2u);\n        let z_radius: i32 = i32(kernelDepth/2u);\n\n        var xyOffset: i32 = 0; //These offsets are for kernelsizes of 1. I didn't wanna rewrite everything else for that case\n        var zOffset: i32 = 0;\n        if (xy_radius == 0){\n            xyOffset = -1;\n        }\n        if (z_radius == 0){\n            zOffset = -1;\n        }\n\n        var xSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n\n        var count: u32 = 0u;\n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = f32(firstData[newIdx]);\n                        let yI = f32(secondData[newIdx]);\n                        xSum += xI;    \n                        ySum += yI;\n                        count ++;\n                    }\n                }\n            }\n        }\n\n\n        let N: f32 = f32(count);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        var numSum: f32 = 0;\n        var denomSum: f32 = 0;\n        \n        for (var kx: i32 = -xy_radius + xyOffset; kx < xy_radius; kx++) {\n            for (var ky: i32 = -xy_radius + xyOffset; ky < xy_radius; ky++) {\n                for (var kz: i32 = -z_radius + zOffset; kz < z_radius; kz++){\n                    let in_coord = vec3<i32>(global_id) + vec3<i32>(kx, ky, kz);\n                    if (in_coord.x >= 0 && in_coord.x < i32(xSize) &&\n                        in_coord.y >= 0 && in_coord.y < i32(ySize) &&\n                        in_coord.z >= 0 && in_coord.z < i32(zSize)) { \n                        let xOffset = kx * i32(xStride);\n                        let yOffset = ky * i32(yStride);\n                        let zOffset = kz * i32(zStride);\n                        let newIdx = i32(globalIdx) + xOffset + yOffset + zOffset;\n                        let xI = f32(firstData[newIdx]);\n                        let yI = f32(secondData[newIdx]);\n                        numSum += (xI - meanX)*(f32(yI) - meanY);\n                        denomSum += (f32(yI) - meanY)*(f32(yI) - meanY);\n                    }\n                }\n            }\n        }\n        outputData[globalIdx] = f16(numSum/denomSum);\n    }\n",eB="\n    enable f16;\n    struct Params {\n        xStride: u32,\n        yStride: u32,\n        zStride: u32,\n        xSize: u32,\n        ySize: u32,\n        zSize: u32,\n        reduceDim: u32,\n        reverse: u32,\n        workGroups: vec3<u32>,\n    };\n    @group(0) @binding(0) var<storage, read> inputData: array<f16>;\n    @group(0) @binding(1) var<storage, read_write> outputData: array<f32>;\n    @group(0) @binding(2) var<uniform> params: Params;\n\n    @compute @workgroup_size(4, 4, 4)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride; \n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let zSize = params.zSize; \n        let reverse = params.reverse;\n        let workGroups = params.workGroups;\n        let reduceDim = params.reduceDim;\n\n        let outX = global_id.x; \n        let outY = global_id.y;\n        let outZ = global_id.z; \n\n        if (outX >= xSize || outY >= ySize || outZ >= zSize) {\n            return;\n        }\n        let totalSize: u32 = xSize * ySize * zSize;\n        var baseIdx = outZ * zStride + outY * yStride + outX * xStride;\n        var accum: f32 = 0;\n\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // CUMSUM along Z\n            if (reverse == u32(1)){\n                baseIdx = (zSize - outZ - 1) * zStride + outY * yStride + outX * xStride;\n            }\n            for (var z: u32 = 0u; z < outZ; z++) {\n                var newZ: u32 = z;\n                if (reverse == u32(1)){\n                    newZ = zSize - z - 1;\n                }\n                let idx = newZ * zStride + outY * yStride + outX * xStride;\n                accum += f32(inputData[idx]);\n            }\n\n        } else if (reduceDim == 1u) { // CUMSUM along Y\n            if (reverse == u32(1)){\n                baseIdx = outZ * zStride + (ySize - outY - 1)* yStride + outX * xStride;\n            }\n            for (var y: u32 = 0u; y < outY; y++) {\n                var newY: u32 = y;\n                if (reverse == u32(1)){\n                    newY = ySize - y - 1;\n                }\n                let idx = outZ * zStride + newY * yStride + outX * xStride;\n                accum += f32(inputData[idx]);\n            }\n        } else { // CUMSUM along X\n             if (reverse == u32(1)){\n                baseIdx = outZ * zStride + outY* yStride + (xSize - outX - 1) * xStride;\n            }\n            for (var x: u32 = 0u; x < outX; x++) {\n                var newX: u32 = x;\n                if (reverse == u32(1)){\n                    newX = xSize - x - 1;\n                }\n                let idx = outZ * zStride + outY * yStride + newX * xStride;\n                accum += f32(inputData[idx]);\n            }\n        }\n            outputData[baseIdx] = accum;\n    }\n",eV="\n    enable f16;\n    struct Params {\n        zStride: u32,\n        yStride: u32,\n        xStride: u32,\n        xSize: u32,\n        ySize: u32,\n        reduceDim: u32,\n        dimLength: u32,\n    };\n    @group(0) @binding(0) var<storage, read> firstData: array<f16>;\n    @group(0) @binding(1) var<storage, read> secondData: array<f16>;\n    @group(0) @binding(2) var<storage, read_write> outputData: array<f16>;\n    @group(0) @binding(3) var<uniform> params: Params;\n\n    @compute @workgroup_size(16, 16, 1)\n    fn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n        let zStride = params.zStride;\n        let yStride = params.yStride;\n        let xStride = params.xStride;\n        let xSize = params.xSize;\n        let ySize = params.ySize;\n        let reduceDim = params.reduceDim;\n        let dimLength = params.dimLength;\n                        \n        let outX = global_id.y;\n        let outY = global_id.x;\n        \n        if (outX >= xSize || outY >= ySize) {\n            return;\n        }\n\n        var xSum: f32 = 0.0;\n        var xxSum: f32 = 0.0;\n        var ySum: f32 = 0.0;\n        var yySum: f32 = 0.0;\n        var xySum: f32 = 0.0;\n        // Iterate along the dimension we're averaging\n        if (reduceDim == 0u) { // Average along Z\n            let cCoord = outX * xStride + outY * yStride;\n            for (var z: u32 = 0u; z < dimLength; z++) {\n                let inputIndex = cCoord + (z * zStride);\n                let xI = f32(firstData[inputIndex]);\n                let yI = f32(secondData[inputIndex]);\n                xSum += xI;\n                xxSum += xI * xI;\n                ySum += yI;\n                yySum += yI * yI;\n                xySum += xI * yI;\n            }\n        } else if (reduceDim == 1u) { // Average along Y\n            let cCoord = outX * xStride + outY * zStride;\n            for (var y: u32 = 0u; y < dimLength; y++) {\n                let inputIndex = cCoord + (y * yStride);\n                let xI = f32(firstData[inputIndex]);\n                let yI = f32(secondData[inputIndex]);\n                xSum += xI;\n                xxSum += xI * xI;\n                ySum += yI;\n                yySum += yI * yI;\n                xySum += xI * yI;\n            }\n        } else { // Average along X\n            let cCoord = outX * yStride + outY * zStride;\n            for (var x: u32 = 0u; x < dimLength; x++) {\n                let inputIndex = cCoord + (x * xStride);\n                let xI = f32(firstData[inputIndex]);\n                let yI = f32(secondData[inputIndex]);\n                xSum += xI;\n                xxSum += xI * xI;\n                ySum += yI;\n                yySum += yI * yI;\n                xySum += xI * yI;\n            }\n        }\n\n        let N: f32 = f32(dimLength);\n        let meanX = xSum / N;\n        let meanY = ySum / N;\n        let varX = (xxSum / N) - (meanX * meanX);\n        let varY = (yySum / N) - (meanY * meanY);\n        let covXY = (xySum / N) - (meanX * meanY);\n        let sigmaX = sqrt(max(0.0, varX));\n        let sigmaY = sqrt(max(0.0, varY));\n        let epsilon = 1e-6;\n        let denominator = sigmaX * sigmaY + epsilon;\n        let correlation = covXY / denominator;\n\n        let outputIndex = outY * xSize + outX;\n        outputData[outputIndex] = f16(correlation);\n    }\n";async function eN(e,n){var t;let i=await (null==(t=navigator.gpu)?void 0:t.requestAdapter()),r=null==i?void 0:i.limits.maxBufferSize,a=null==i?void 0:i.limits.maxStorageBufferBindingSize,o=!!i&&i.features.has("shader-f16"),l=o?await (null==i?void 0:i.requestDevice({requiredFeatures:["shader-f16"],requiredLimits:{maxBufferSize:r,maxStorageBufferBindingSize:a}})):await (null==i?void 0:i.requestDevice({requiredLimits:{maxBufferSize:r,maxStorageBufferBindingSize:a}}));if(!l)return void Error("need a browser that supports WebGPU");let u=n[0]*n[1]*n[2],s=l.createBuffer({label:"Input Buffer",size:e.byteLength*(o?1:2),usage:GPUBufferUsage.COPY_SRC|GPUBufferUsage.COPY_DST}),d=l.createBuffer({label:"Output Buffer",size:u*(o?2:4),usage:GPUBufferUsage.MAP_READ|GPUBufferUsage.COPY_DST});l.queue.writeBuffer(s,0,o?e:new Float32Array(e));let c=l.createCommandEncoder();c.copyBufferToBuffer(s,0,d,0,u*(o?2:4));let f=c.finish();l.queue.submit([f]),await d.mapAsync(GPUMapMode.READ);let m=d.getMappedRange(),x=new Float16Array(o?m.slice():new Float32Array(m.slice()));return d.unmap(),x}async function eT(e,n,t,a){var o;let l=await (null==(o=navigator.gpu)?void 0:o.requestAdapter()),u=null==l?void 0:l.limits.maxBufferSize,s=null==l?void 0:l.limits.maxStorageBufferBindingSize,d=!!l&&l.features.has("shader-f16"),c=d?await (null==l?void 0:l.requestDevice({requiredFeatures:["shader-f16"],requiredLimits:{maxBufferSize:u,maxStorageBufferBindingSize:s}})):await (null==l?void 0:l.requestDevice({requiredLimits:{maxBufferSize:u,maxStorageBufferBindingSize:s}}));if(!c)return void Error("need a browser that supports WebGPU");let{kernelDepth:f,kernelSize:m}=a,{strides:x,shape:p}=n,y=p[0]*p[1]*p[2],[S,g,v]=x,z=p.map(e=>Math.ceil(e/4)),h=(d?r:i).StDevConvolution,_=c.createShaderModule({label:"convolution compute module",code:h}),b=c.createComputePipeline({label:"convolution compute pipeline",layout:"auto",compute:{module:_}}),k=(0,G.Wu)(h),w=(0,G.FE)(k.uniforms.params);w.set({xStride:v,yStride:g,zStride:S,xSize:p[2],ySize:p[1],zSize:p[0],workGroups:[z[2],z[1],z[0]],kernelDepth:f,kernelSize:m});let I=c.createBuffer({label:"Input Buffer",size:e.byteLength*(d?1:2),usage:GPUBufferUsage.STORAGE|GPUBufferUsage.COPY_DST}),D=c.createBuffer({label:"Output Buffer",size:y*(d?2:4),usage:GPUBufferUsage.STORAGE|GPUBufferUsage.COPY_SRC}),C=c.createBuffer({label:"Uniform Buffer",size:w.arrayBuffer.byteLength,usage:GPUBufferUsage.UNIFORM|GPUBufferUsage.COPY_DST}),M=c.createBuffer({label:"Read Buffer",size:y*(d?2:4),usage:GPUBufferUsage.COPY_DST|GPUBufferUsage.MAP_READ});c.queue.writeBuffer(I,0,d?e:new Float32Array(e)),c.queue.writeBuffer(C,0,w.arrayBuffer);let O=c.createBindGroup({layout:b.getBindGroupLayout(0),entries:[{binding:0,resource:{buffer:I}},{binding:1,resource:{buffer:D}},{binding:2,resource:{buffer:C}}]}),P=c.createCommandEncoder({label:"convolution encoder"}),Y=P.beginComputePass({label:"convolution compute pass"});Y.setPipeline(b),Y.setBindGroup(0,O),Y.dispatchWorkgroups(z[2],z[1],z[0]),Y.end(),P.copyBufferToBuffer(D,0,M,0,y*(d?2:4)),c.queue.submit([P.finish()]),await M.mapAsync(GPUMapMode.READ);let j=M.getMappedRange(),X=new Float16Array(d?j.slice():new Float32Array(j.slice()));return M.unmap(),X}async function eE(e,n,t){let{kernelDepth:i,kernelSize:r}=t,{strides:a,shape:o}=n,l=o[0]*o[1]*o[2],[u,s,d]=a,c={xStride:d,yStride:s,zStride:u,xSize:o[2],ySize:o[1],zSize:o[0],kernelSize:r,kernelDepth:i};!function(e,n,t){let{xStride:i,yStride:r,zStride:a,xSize:o,ySize:l,zSize:u,kernelSize:s,kernelDepth:d}=t,c=Math.floor(s/2),f=Math.floor(d/2),m=0===c?-1:0,x=0===f?-1:0;for(let t=0;t<u;t++)for(let s=0;s<l;s++)for(let d=0;d<o;d++){let p=t*a+s*r+d*i,y=0,S=0;for(let n=-c+m;n<c;n++)for(let g=-c+m;g<c;g++)for(let c=-f+x;c<f;c++){let f=d+n,m=s+g,x=t+c;f>=0&&f<o&&m>=0&&m<l&&x>=0&&x<u&&(y+=e[p+n*i+g*r+c*a],S++)}let g=y/S,v=0;for(let n=-c+m;n<c;n++)for(let y=-c+m;y<c;y++)for(let c=-f+x;c<f;c++){let f=d+n,m=s+y,x=t+c;if(f>=0&&f<o&&m>=0&&m<l&&x>=0&&x<u){let t=g-e[p+n*i+y*r+c*a];v+=t*t}}let z=Math.sqrt(v/S);n[p]=z}}(e,new Float32Array(l),c)}let eR=e=>{let{setTexture:n,ZarrDS:t}=e,{strides:i,dataShape:r,valueScales:a,isFlat:o,setShowLoading:l,setValueScales:u}=(0,c.on)((0,f.k)(e=>({strides:e.strides,dataShape:e.dataShape,valueScales:e.valueScales,isFlat:e.isFlat,setIsFlat:e.setIsFlat,setDownloading:e.setDownloading,setShowLoading:e.setShowLoading,setValueScales:e.setValueScales})));(0,c.Ws)(e=>e.setPlotType);let{useCPU:d,execute:m,getBufferSpeed:x,valueScalesOrig:y,kernelSize:S,kernelDepth:g,kernelOperation:v,analysisStore:z,analysisMode:h,analysisArray:_,setCpuTime:b,setGpuTime:k,setValueScalesOrig:w,setAnalysisArray:I,setBufferSpeed:D}=(0,c.r2)((0,f.k)(e=>({axis:e.axis,execute:e.execute,getBufferSpeed:e.getBufferSpeed,useCPU:e.useCPU,valueScalesOrig:e.valueScalesOrig,kernelSize:e.kernelSize,kernelDepth:e.kernelDepth,kernelOperation:e.kernelOperation,reverseDirection:e.reverseDirection,analysisStore:e.analysisStore,analysisMode:e.analysisMode,analysisArray:e.analysisArray,setCpuTime:e.setCpuTime,setGpuTime:e.setGpuTime,setValueScalesOrig:e.setValueScalesOrig,setAnalysisArray:e.setAnalysisArray,setBufferSpeed:e.setBufferSpeed})));return(0,c.SZ)(e=>e.slice),(0,s.useRef)(new Float32Array(1)),(0,s.useEffect)(()=>{let e=(0,p.Td)(z);if(e.length<=1||o)return;async function n(n){if(x)for(let n=0;n<20;n++)await eN(e,r);else if(n)for(let n=0;n<20;n++)await eE(e,{shape:r,strides:i},{kernelDepth:g,kernelSize:S});else{console.log("not here");for(let n=0;n<20;n++)await eT(e,{shape:r,strides:i},"StDevConvolution",{kernelDepth:g,kernelSize:S})}return null}l(!0);let t=Date.now();n(d).then(()=>{let e=(Date.now()-t)/20/1e3;console.log(e),x?D(e):d?b(e):(console.log("not here2"),k(e)),l(!1)})},[m]),null},eU=e=>{let{show:n}=e,{valueScales:t,variable:i,metadata:r}=(0,c.on)((0,f.k)(e=>({valueScales:e.valueScales,variable:e.variable,metadata:e.metadata}))),{exportImg:l,enableExport:u,includeBackground:d,includeColorbar:x,doubleSize:p,useCustomRes:y,getCbarLoc:S,getCbarNum:g,getCustomRes:v,setHideAxisControls:z,setHideAxis:_}=(0,c.qi)((0,f.k)(e=>({exportImg:e.exportImg,enableExport:e.enableExport,includeBackground:e.includeBackground,includeColorbar:e.includeColorbar,doubleSize:e.doubleSize,useCustomRes:e.useCustomRes,getCbarLoc:e.getCbarLoc,getCbarNum:e.getCbarNum,getCustomRes:e.getCustomRes,setHideAxisControls:e.setHideAxisControls,setHideAxis:e.setHideAxis}))),{gl:b,scene:k,camera:w}=(0,m.C)(),I=(0,h.$m)("--text-plot"),D=(0,h.$m)("--background");return(0,s.useEffect)(()=>{if(!n||!u)return;let e=b.domElement.width,a=b.domElement.height,l=v(),s=y?l[0]:p?2*e:e,c=y?l[1]:p?2*a:a,f=document.createElement("canvas"),m=f.getContext("2d");if(!m)return;if(f.width=s,f.height=c,d&&(m.fillStyle=D,m.fillRect(0,0,f.width,f.height)),y){let e,n=b.getSize(new o.I9Y);if(w instanceof o.ubm)e={aspect:w.aspect},w.aspect=s/c,w.updateProjectionMatrix();else if(w instanceof o.qUd){e={left:w.left,right:w.right,top:w.top,bottom:w.bottom};let n=s/c;if(n>(w.right-w.left)/(w.top-w.bottom)){let e=(w.top-w.bottom)*n,t=(w.left+w.right)/2;w.left=t-e/2,w.right=t+e/2}else{let e=(w.right-w.left)/n,t=(w.top+w.bottom)/2;w.top=t+e/2,w.bottom=t-e/2}w.updateProjectionMatrix()}b.setSize(s,c),b.render(k,w),m.drawImage(b.domElement,0,0,s,c),w instanceof o.ubm?w.aspect=e.aspect:w instanceof o.qUd&&(w.left=e.left,w.right=e.right,w.top=e.top,w.bottom=e.bottom),b.setSize(n.x,n.y),w.updateProjectionMatrix(),_(!1),z(!1),b.render(k,w)}else b.render(k,w),m.drawImage(b.domElement,0,0,s,c),b.render(k,w),_(!1),z(!1);m.fillStyle=I,m.font="".concat(p?72:36,'px "Segoe UI"'),m.fillText(i,p?40:20,p?100:50);let h=p?52:26;if(x){let n=document.getElementById("colorbar-canvas"),i=S(),a=p?Math.min(1024,.8*e):Math.min(512,.8*e),o=p?48:24,l=Math.round(s/2-a/2),u="top"===i?p?140:70:p?c-140:c-70,d="right"===i||"left"===i;if(d){let e=a;a=o,u=Math.round(c/2-(o=e)/2),l="right"===i?p?s-140:s-70:p?140:70}if(n instanceof HTMLCanvasElement)if(d){m.save();let e=l+a/2,t=u+o/2;m.translate(e,t),m.rotate(-Math.PI/2);let i=p?1024:512,r=p?48:24;m.drawImage(n,-i/2,-r/2,i,r),m.restore()}else m.drawImage(n,l,u,a,o);let f=g(),x=t.maxVal-t.minVal,y=1/(f-1),v=d?1/(f-1)*o:1/(f-1)*a;if(m.font="".concat(p?36:18,'px "Segoe UI"'),d){m.textBaseline="middle",m.textAlign="left"==i?"left":"right";for(let e=0;e<f;e++)"left"==i?m.fillText(String((t.minVal+e*y*x).toFixed(2)),l+a+6,u+o-e*v):m.fillText(String((t.minVal+e*y*x).toFixed(2)),l-6,u+o-e*v)}else{m.textBaseline="top",m.textAlign="center";for(let e=0;e<f;e++)m.fillText(String((t.minVal+e*y*x).toFixed(2)),l+e*v,u+o+6)}m.fillStyle=I,m.font="".concat(h,'px "Segoe UI" bold'),m.textAlign="center",m.fillText(null==r?void 0:r.units,l+a/2,u-h-4)}m.fillStyle="#888888",m.font="".concat(p?40:20,'px "Segoe UI", serif '),m.textAlign="left",m.textBaseline="bottom",m.fillText("browzarr.io",p?20:10,p?c-20:c-10),f.toBlob(e=>{if(!e)return;let n=URL.createObjectURL(e),t=document.createElement("a");t.download="browzarr-plot.png",t.href=n,t.click(),URL.revokeObjectURL(n)},"image/png")},[l]),(0,a.jsx)(a.Fragment,{})},eG=e=>{let{isFlat:n}=e,{resetCamera:t}=(0,c.Ws)((0,f.k)(e=>({resetCamera:e.resetCamera}))),i=(0,s.useRef)(null),r=(0,s.useRef)(!1);return(0,s.useEffect)(()=>{if(!r.current){r.current=!0;return}if(i.current){let e,t=i.current,r=performance.now(),a=t.object.position.clone(),l=t.position0.clone(),u=t.target.clone(),s=t.target0.clone(),d=t.object.zoom,c=i=>{(0,m.m)();let f=Math.min((i-r)/1e3,1);t.object.position.lerpVectors(a,l,f),t.target.lerpVectors(u,s,f),n&&(t.object.zoom=o.cj9.lerp(d,1e3,f),t.object.updateProjectionMatrix(),t.update()),f<1&&(e=requestAnimationFrame(c))};return e=requestAnimationFrame(c),()=>cancelAnimationFrame(e)}},[t]),(0,a.jsxs)(a.Fragment,{children:[n&&(0,a.jsx)(T.N,{ref:i,enableRotate:!1,enablePan:!0,maxDistance:50,minZoom:50,maxZoom:3e3}),!n&&(0,a.jsx)(T.N,{ref:i,enableRotate:!0,enablePan:!0,maxDistance:50})]})},eF=e=>{let{ZarrDS:n}=e,{setShape:t,setDataShape:i,setFlipY:r,setValueScales:l,setMetadata:u,setDimArrays:d,setDimNames:m,setDimUnits:y,setPlotOn:g,setShowLoading:_}=(0,c.on)((0,f.k)(e=>({setShape:e.setShape,setDataShape:e.setDataShape,setFlipY:e.setFlipY,setValueScales:e.setValueScales,setMetadata:e.setMetadata,setDimArrays:e.setDimArrays,setDimNames:e.setDimNames,setDimUnits:e.setDimUnits,setPlotOn:e.setPlotOn,setShowLoading:e.setShowLoading}))),{colormap:b,variable:k,isFlat:w,metadata:I,valueScales:D,is4D:C,setIsFlat:M}=(0,c.on)((0,f.k)(e=>({colormap:e.colormap,variable:e.variable,isFlat:e.isFlat,metadata:e.metadata,valueScales:e.valueScales,is4D:e.is4D,setIsFlat:e.setIsFlat}))),{plotType:O}=(0,c.Ws)((0,f.k)(e=>({plotType:e.plotType}))),{slice:P,reFetch:Y}=(0,c.SZ)((0,f.k)(e=>({slice:e.slice,reFetch:e.reFetch}))),j=(0,s.useRef)([0,0]),X=(0,s.useRef)(0),[A,L]=(0,s.useState)(!1),[B,V]=(0,s.useState)([0,0]),[N,T]=(0,s.useState)(null),[E,G]=(0,s.useState)(!0);(0,s.useEffect)(()=>{if("Default"!=k){_(!0),G(!1);try{n.GetArray(k,P).then(e=>{let[n,r]=(0,R.oW)({data:e.data,shape:e.shape});if(n instanceof o.GYF||n instanceof o.dYF?T(n):(console.error("Invalid texture type returned from ArrayToTexture"),T(null)),e.scalingFactor){let{maxVal:n,minVal:t}=r;l({maxVal:n*Math.pow(10,e.scalingFactor),minVal:t*Math.pow(10,e.scalingFactor)})}else l(r);2==e.shape.length?M(!0):M(!1);let a=e.shape[1]/e.shape[2]*2;t(new o.Pq0(2,a,2)),i(e.shape),G(!0),g(!0),_(!1)})}catch(e){_(!1);return}n.GetAttributes(k).then(e=>{u(e);let[t,i,a]=n.GetDimArrays();C&&(t=t.slice(1),i=i.slice(1),a=a.slice(1)),d(t),m(a),t.length>2?t[1][1]<t[1][0]?r(!0):r(!1):t[0][1]<t[0][0]?r(!0):r(!1);let o=[];for(let e of i)o.push(e.units);y(o),(0,p.QB)(o,t)})}else u(null)},[Y]);let F=(0,s.useMemo)(()=>({setLoc:V,setShowInfo:L,coords:j,val:X}),[]),Z=(0,s.useMemo)(()=>h.Fp,[]);return(0,a.jsxs)("div",{className:"main-canvas",style:{width:"100vw"},children:[(0,a.jsx)(eR,{setTexture:T,ZarrDS:n}),E&&(0,a.jsx)(h.PL,{units:null==I?void 0:I.units,valueScales:D}),(0,a.jsx)(Z,{}),(w||"flat"==O)&&(0,a.jsx)(U,{loc:B,show:A,info:[...j.current,X.current]}),(!w&&"flat"!=O||w&&"sphere"===O)&&(0,a.jsx)(a.Fragment,{children:(0,a.jsxs)(z.Hl,{id:"main-canvas",camera:{position:w?[0,0,5]:[-4.5,3,4.5],fov:50},frameloop:"demand",gl:{preserveDrawingBuffer:!0},children:[(0,a.jsx)(e0,{}),(0,a.jsx)(eU,{show:E}),E&&(0,a.jsx)(e6,{}),"volume"==O&&E&&(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(x,{volTexture:N}),(0,a.jsx)(S,{ZarrDS:n})]}),"point-cloud"==O&&E&&(0,a.jsx)(a.Fragment,{children:(0,a.jsx)(v,{textures:{texture:N,colormap:b},ZarrDS:n})}),"sphere"==O&&E&&(0,a.jsx)(eQ,{texture:N,ZarrDS:n}),(0,a.jsx)(eG,{isFlat:!1})]})}),(w||!w&&"flat"==O)&&(0,a.jsx)(a.Fragment,{children:(0,a.jsxs)(z.Hl,{id:"main-canvas",camera:{position:[0,0,5],zoom:1e3},orthographic:!0,frameloop:"demand",children:[(0,a.jsx)(eU,{show:E}),(0,a.jsx)(e0,{}),E&&(0,a.jsx)(e6,{}),(0,a.jsx)(eW,{texture:N,infoSetters:F}),(0,a.jsx)(eG,{isFlat:!0})]})})]})};function eZ(e){let{points:n,tsID:t,pointSetters:i,scalers:r}=e,l=(0,s.useRef)(null),u=n.length,d=(0,s.useRef)(null),[x,p]=(0,s.useState)(!1),{setPointID:y,setPointLoc:S,setShowPointInfo:g}=i,[v,z]=(0,s.useState)(1),{pointColor:h,pointSize:_,useCustomPointColor:b}=(0,c.Ws)((0,f.k)(e=>({pointColor:e.pointColor,pointSize:e.linePointSize,showPoints:e.showPoints,useCustomPointColor:e.useCustomPointColor}))),{xScale:k,yScale:w}=r,{timeSeries:I}=(0,c.on)((0,f.k)(e=>({timeSeries:e.timeSeries}))),[D,C,M]=I[t].color,O=(0,s.useMemo)(()=>new o.Gu$(_),[_]),P=(0,s.useMemo)(()=>new o.V9B({color:new o.Q1f().setRGB(D/300,C/300,M/300).convertSRGBToLinear()}),[h,b,I]);return(0,s.useEffect)(()=>{if(l.current){let e=new o.B69;for(let t=0;t<u;t++){let i=n[t].toArray();e.position.set(i[0]*(k/2),i[1]*w,1),e.scale.set(_/v,_/v,_/v),e.updateMatrix(),l.current.setMatrixAt(t,e.matrix)}l.current.instanceMatrix.needsUpdate=!0}},[n,v,O,P,k,w,_]),(0,m.D)(e=>{let{camera:n}=e;n.zoom!==v&&z(n.zoom)}),(0,a.jsx)(a.Fragment,{children:(0,a.jsx)("mesh",{position:[0,0,5],onPointerEnter:function(e){if(l.current){let n=e.instanceId,i=new o.B69,r=new o.kn4,a=new o.Pq0;n!=d.current&&d.current&&(l.current.getMatrixAt(d.current,r),a.setFromMatrixPosition(r),i.scale.set(_/v,_/v,_/v),i.position.copy(a),i.updateMatrix(),l.current.setMatrixAt(d.current,i.matrix)),d.current=n,l.current.getMatrixAt(n,r),a.setFromMatrixPosition(r),i.scale.set(3*_/v,3*_/v,3/v),i.position.copy(a),i.updateMatrix(),l.current.setMatrixAt(e.instanceId,i.matrix),l.current.instanceMatrix.needsUpdate=!0,p(e=>!e),y([t,e.instanceId]),S([e.clientX,e.clientY]),g(!0)}},onPointerLeave:function(e){let n=e.instanceId;if(l.current){let e=new o.B69,t=new o.kn4,i=new o.Pq0;d.current&&(l.current.getMatrixAt(n,t),i.setFromMatrixPosition(t),e.scale.set(_/v,_/v,_/v),e.position.copy(i),e.updateMatrix(),l.current.setMatrixAt(n,e.matrix),l.current.instanceMatrix.needsUpdate=!0,p(e=>!e),g(n!=d.current))}},children:(0,a.jsx)("instancedMesh",{ref:l,args:[O,P,u]})})})}let eq=e=>{let{height:n,xScale:t,yScale:i,pointSetters:r}=e,{valueScales:l,timeSeries:u,colormap:d}=(0,c.on)((0,f.k)(e=>({valueScales:e.valueScales,timeSeries:e.timeSeries,colormap:e.colormap}))),{lineWidth:x,useLineColor:p,lineColor:y,showPoints:S,lineResolution:g,useCustomColor:v}=(0,c.Ws)((0,f.k)(e=>({lineWidth:e.lineWidth,linePointSize:e.linePointSize,showPoints:e.showPoints,useLineColor:e.useLineColor,lineColor:e.lineColor,lineResolution:e.lineResolution,useCustomColor:e.useCustomColor}))),{camera:z}=(0,m.C)(),{maxVal:h,minVal:_}=l,b=(0,s.useMemo)(()=>{let e={};return Object.keys(u).reverse().map((n,r)=>{let[a,l,s]=u[n].color;e[n]=new o.BKk({glslVersion:o.Wdf,uniforms:{cmap:{value:d},xScale:{value:t},yScale:{value:i},aspect:{value:window.innerWidth/window.innerHeight},thickness:{value:x},miter:{value:1},useLineColor:{value:v},useMapColors:{value:p},lineColor:{value:new o.Q1f().setRGB(a/255,l/255,s/255)},userColor:{value:new o.Q1f(y)},zoom:{value:z.zoom}},vertexShader:"attribute float direction; \nattribute vec3 next;\nattribute vec3 previous;\nattribute float normed;\n\nvarying float vNormed;\n\nuniform float zoom;\nuniform float thickness;\nuniform int miter;\nuniform float xScale; \nuniform float yScale;\n\nfloat getOrthographicZoom(mat4 projectionMatrix, float referenceWidth) {\n    float m0 = projectionMatrix[0][0]; // Scaling factor: 2/(right-left)\n    float viewWidth = 2.0 / m0; // Viewable width\n    return referenceWidth / viewWidth; // Zoom level\n}\n\nvoid main() {\n    vec3 pos = position;\n    vec3 prev = previous;\n    vec3 nex = next;\n    pos.x *= xScale/2.0;\n    pos.y *= yScale;\n    prev.x *= xScale/2.0;\n    prev.y *= yScale;\n    nex.x *= xScale/2.0;\n    nex.y *= yScale;\n\n    // Transform positions to view space (before projection)\n    float zoom = getOrthographicZoom(projectionMatrix, 2.);\n    float zoomLevel = 2. / projectionMatrix[0][0]; // Extract vertical scale\n    vec4 currentView = modelViewMatrix * vec4(pos, 1.0);\n    vec4 prevView = modelViewMatrix * vec4(prev, 1.0);\n    vec4 nextView = modelViewMatrix * vec4(nex, 1.0);\n\n    // Compute directions in view space\n    vec3 dir = vec3(0.0);\n    if (currentView.xyz == prevView.xyz) {\n        dir = normalize(nextView.xyz - currentView.xyz);\n    } else if (currentView.xyz == nextView.xyz) {\n        dir = normalize(currentView.xyz - prevView.xyz);\n    } else {\n        vec3 dirA = normalize(currentView.xyz - prevView.xyz);\n        if (miter == 1) {\n            vec3 dirB = normalize(nextView.xyz - currentView.xyz);\n            vec3 tangent = normalize(dirA + dirB);\n            vec3 perp = vec3(-dirA.y, dirA.x, 0.0); // Perpendicular in view space\n            vec3 miterVec = vec3(-tangent.y, tangent.x, 0.0);\n            float miterLen = dot(miterVec, perp);\n            miterLen = max(miterLen, 0.5); // Avoid division by zero\n            dir = tangent;\n        } else {\n            dir = dirA;\n        }\n    }\n\n    // Compute normal in view space\n    vec3 normal = vec3(-dir.y, dir.x, 0.0); // Perpendicular to direction\n    float len = thickness / zoom/500.; // Thickness in world/view space units\n    normal *= 0.5 * len * direction; // Apply thickness and direction\n\n    // Apply offset in view space\n    currentView.xyz += normal;\n\n    // Project to clip space\n    gl_Position = projectionMatrix * currentView;\n\n\n    vNormed = normed;\n    gl_PointSize = 1.0;\n}",fragmentShader:"\n                out vec4 Color;\n                uniform sampler2D cmap;\n                uniform bool useLineColor;\n                uniform bool useMapColors;\n                uniform vec3 lineColor;\n                uniform vec3 userColor;\n                varying float vNormed;\n\n                void main() {\n                    vec4 texColor = texture(cmap, vec2(vNormed, 0.1));\n                    texColor.a = 1.;\n                    Color = useLineColor ? vec4(userColor, 1.0) : useMapColors ? texColor : vec4(lineColor, 1.0) ;\n                }\n                ",depthWrite:!1})}),e},[u]);(0,s.useEffect)(()=>{if(Object.values(b))for(let e of Object.values(b)){let n=e.uniforms;n.cmap.value=d,n.xScale.value=t,n.yScale.value=i,n.aspect.value=window.innerWidth/window.innerHeight,n.thickness.value=x,n.miter.value=1,n.useLineColor.value=v,n.useMapColors.value=p,n.userColor.value=new o.Q1f(y),n.zoom.value=z.zoom,(0,m.m)()}},[d,x,t,i,window.innerWidth,window.innerHeight,p,y,z.zoom,v]);let k=(0,s.useMemo)(()=>window.innerWidth,[window.innerWidth]),w=(0,s.useMemo)(()=>window.innerHeight-n,[window.innerWidth,n]),[I,D]=(0,s.useState)({}),C=(0,s.useMemo)(()=>{let e={},n={};return Object.keys(u).map((t,i)=>{let r=u[t].data,a=r.map(e=>(e-_)/(h-_)),l=function(e,n,t){let i=(n-e)/(t-1);return Array.from({length:t},(n,t)=>e+i*t)}(-k,k,r.length),s=a.map((e,n)=>new o.Pq0(l[n],(e-.5)*w,5));n[t]=s;let d=new o.B6O(s).getPoints(s.length*g-1),c=[];for(let e=0;e<d.length;e++){let n=e/(d.length-1)*(a.length-1),t=Math.floor(n),i=Math.min(a.length-1,Math.ceil(n)),r=n-t;c.push(a[t]*(1-r)+a[i]*r)}let f=d.length,m=[],x=[],p=[],y=[],S=[],v=[];for(let e=0;e<f;e++){let n=d[e],t=d[Math.max(0,e-1)],i=d[Math.min(f-1,e+1)];m.push(...n,...n),x.push(1,-1),p.push(...t,...t),y.push(...i,...i),S.push(c[e],c[e])}for(let e=0;e<f-1;e++){let n=2*e,t=n+1,i=n+2,r=n+3;v.push(n,t,i),v.push(t,r,i)}let z=new o.LoY;z.setAttribute("position",new o.qtW(m,3)),z.setAttribute("direction",new o.qtW(x,1)),z.setAttribute("previous",new o.qtW(p,3)),z.setAttribute("next",new o.qtW(y,3)),z.setAttribute("normed",new o.qtW(S,1)),z.setIndex(new o.A$4(v,1)),e[t]=z}),D(n),e},[u,g]);return(0,s.useEffect)(()=>{(0,m.m)()},[S]),(0,a.jsx)(a.Fragment,{children:(0,a.jsxs)("group",{children:[Object.keys(u).map((e,n)=>(0,a.jsx)("mesh",{geometry:C[e],material:b[e]},"lineMesh_".concat(n))),S&&Object.keys(u).map((e,n)=>(0,a.jsx)(eZ,{points:I[e],tsID:e,pointSetters:r,scalers:{xScale:t,yScale:i}},"plotPoints_".concat(n)))]})})},eW=e=>{let{texture:n,infoSetters:t}=e,{setLoc:i,setShowInfo:r,val:l,coords:u}=t,{flipY:d,colormap:m,valueScales:x,dimArrays:y,isFlat:S}=(0,c.on)((0,f.k)(e=>({flipY:e.flipY,colormap:e.colormap,valueScales:e.valueScales,dimArrays:e.dimArrays,isFlat:e.isFlat}))),{cScale:g,cOffset:v,animProg:z,nanTransparency:h,nanColor:_}=(0,c.Ws)((0,f.k)(e=>({cOffset:e.cOffset,cScale:e.cScale,resetAnim:e.resetAnim,animate:e.animate,animProg:e.animProg,nanTransparency:e.nanTransparency,nanColor:e.nanColor}))),{axis:b,analysisMode:k,analysisArray:w}=(0,c.r2)((0,f.k)(e=>({axis:e.axis,analysisMode:e.analysisMode,analysisArray:e.analysisArray}))),I=n.source.data,D=(0,s.useMemo)(()=>I.height/I.width,[I]),C=(0,s.useMemo)(()=>new o.bdM(2,2*D),[D]),M=(0,s.useRef)(!1),O=(0,s.useRef)(new o.I9Y(0,0)),P=k&&2==b,Y=(0,s.useMemo)(()=>k?w:(0,p.Td)(),[k,w]),j=(0,s.useMemo)(()=>y.length>2?y.filter((e,n)=>n!=b):y,[y,b]),X=(0,s.useMemo)(()=>new o.BKk({glslVersion:o.Wdf,uniforms:{cScale:{value:g},cOffset:{value:v},data:{value:n},cmap:{value:m},animateProg:{value:z},nanColor:{value:new o.Q1f(_)},nanAlpha:{value:1-h}},vertexShader:" // by Jeran Poehls\r\nvarying vec2 vUv;\r\n\r\nvoid main() {\r\n    vUv = uv;\r\n    gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\r\n}",fragmentShader:S?" // Basic Shader for colors on a plane used in FlatMap with 2D data\r\nprecision highp float;\r\nprecision highp sampler3D;\r\n\r\nout vec4 color;\r\n\r\nvarying vec2 vUv;\r\n\r\nuniform sampler2D data;\r\nuniform sampler2D cmap;\r\nuniform float nanAlpha;\r\nuniform vec3 nanColor;\r\nuniform float cOffset;\r\nuniform float cScale;\r\n\r\nvoid main(){\r\n\r\n    float strength = texture2D(data, vUv).r;\r\n    bool isNaN = strength == 1.;\r\n    float sampLoc = isNaN ? strength: (strength - 0.5)*cScale + 0.5;\r\n    sampLoc = isNaN ? strength : min(sampLoc+cOffset,0.995);\r\n    color = isNaN ? vec4(nanColor, nanAlpha) : vec4(texture2D(cmap, vec2(sampLoc, 0.5)).rgb, 1.);\r\n\r\n}":"//This is for Flat Textures but with 3D textures to sample from i,e; animation\r\n\r\nuniform sampler3D data;\r\nuniform sampler2D cmap;\r\n\r\nuniform float cOffset;\r\nuniform float cScale;\r\nuniform float animateProg;\r\nuniform float nanAlpha;\r\nuniform vec3 nanColor;\r\n\r\nvarying vec2 vUv;\r\nout vec4 Color;\r\n\r\nvoid main() {\r\n\r\n    float strength = texture(data,vec3(vUv, animateProg)).r;\r\n    bool isNaN = strength == 1.;\r\n    float sampLoc = isNaN ? strength: (strength)*cScale;\r\n    sampLoc = isNaN ? strength : min(sampLoc+cOffset,0.995);\r\n    Color = isNaN ? vec4(nanColor, nanAlpha) : vec4(texture2D(cmap, vec2(sampLoc, 0.5)).rgb, 1.);\r\n\r\n}",side:o.$EB}),[S]);(0,s.useEffect)(()=>{if(X){let e=X.uniforms;e.cOffset.value=v,e.data.value=n,e.cmap.value=m,e.animateProg.value=z,e.nanColor.value=new o.Q1f(_),e.nanAlpha.value=1-h,e.cScale.value=g}},[g,v,n,m,z,_,h]),(0,s.useEffect)(()=>{C.dispose()},[C]);let A=(0,s.useRef)(null),L=(0,s.useCallback)(e=>{if(M.current&&e.uv){A.current=e,i([e.clientX,e.clientY]),O.current=e.uv;let{x:n,y:t}=e.uv,r=S?k?j[1].length:y[1].length:y[2].length,a=S?k?j[0].length:y[0].length:y[1].length,o=Math.round(n*r-.5),s=Math.round(t*a-.5),d=r*s+o;d+=S?0:Math.floor((y[0].length-1)*z)*r*a;let c=Y?Y[d]:0;l.current=S&&!k?function(e,n){return e*(n.maxVal-n.minVal)+n.minVal}(c,x):c,u.current=S?k?[j[0][s],j[1][o]]:[y[0][s],y[1][o]]:[y[1][s],y[2][o]]}},[Y,y,z]);return(0,a.jsx)(a.Fragment,{children:(0,a.jsx)("mesh",{material:X,geometry:C,scale:[k&&2==b&&d?-1:1,d||k&&2==b?-1:1,1],rotation:[0,0,P?Math.PI/2:0],onPointerEnter:()=>{r(!0),M.current=!0},onPointerLeave:()=>{r(!1),M.current=!1},onPointerMove:L})})};function eH(e){return e*Math.PI/180}let eQ=e=>{let{texture:n,ZarrDS:t}=e,{colormap:i,isFlat:r}=(0,c.on)((0,f.k)(e=>({colormap:e.colormap,isFlat:e.isFlat}))),{setPlotDim:l,updateDimCoords:u,updateTimeSeries:m}=(0,c.on)((0,f.k)(e=>({setPlotDim:e.setPlotDim,updateDimCoords:e.updateDimCoords,updateTimeSeries:e.updateTimeSeries}))),{analysisMode:x,analysisArray:S}=(0,c.r2)((0,f.k)(e=>({analysisMode:e.analysisMode,analysisArray:e.analysisArray}))),{dimArrays:g,dimNames:v,dimUnits:z,timeSeries:h,dataShape:_,strides:b}=(0,c.on)((0,f.k)(e=>({dimArrays:e.dimArrays,dimNames:e.dimNames,dimUnits:e.dimUnits,timeSeries:e.timeSeries,dataShape:e.dataShape,strides:e.strides}))),{animate:k,animProg:w,cOffset:I,cScale:D,selectTS:C,lonExtent:M,latExtent:O,lonResolution:P,latResolution:Y,nanColor:j,nanTransparency:X,getColorIdx:A,incrementColorIdx:L}=(0,c.Ws)((0,f.k)(e=>({animate:e.animate,animProg:e.animProg,cOffset:e.cOffset,cScale:e.cScale,selectTS:e.selectTS,lonExtent:e.lonExtent,latExtent:e.latExtent,lonResolution:e.lonResolution,latResolution:e.latResolution,nanColor:e.nanColor,nanTransparency:e.nanTransparency,getColorIdx:e.getColorIdx,incrementColorIdx:e.incrementColorIdx}))),[B,V]=(0,s.useState)({}),[N,T]=(0,s.useState)(Array(10).fill(new o.IUQ(-1,-1,-1,-1))),{height:E,width:R}=(0,s.useMemo)(()=>null==n?void 0:n.source.data,[n]);(0,s.useEffect)(()=>{let e=Object.keys(B),n=Object.keys(h),t=(e=e.filter(e=>n.includes(e))).map(e=>B[e]);T([...t,...Array(Math.max(0,10-t.length)).fill(new o.IUQ(-1,-1,-1,-1))])},[B,h]);let[U,G]=(0,s.useMemo)(()=>{let e=Y/2,n=P/2;return[[Math.max(M[0]-n,-180),Math.min(M[1]+n,180)],[Math.max(O[0]-e,-90),Math.min(O[1]+e,90)]]},[O,M,P,Y]),F=(0,s.useMemo)(()=>new o.WBB(1,9),[]),Z=(0,s.useMemo)(()=>new o.BKk({glslVersion:o.Wdf,uniforms:{map:{value:n},selectTS:{value:C},selectBounds:{value:N},cmap:{value:i},cOffset:{value:I},cScale:{value:D},animateProg:{value:w},latBounds:{value:new o.I9Y(eH(G[0]),eH(G[1]))},lonBounds:{value:new o.I9Y(eH(U[0]),eH(U[1]))},nanColor:{value:new o.Q1f(j)},nanAlpha:{value:1-X}},vertexShader:d,fragmentShader:r?" // by Jeran Poehls\nprecision highp float;\nprecision highp sampler2D;\n\nout vec4 color;\n\nin vec3 aPosition;\n\nuniform sampler2D map;\nuniform sampler2D cmap;\n\nuniform float cOffset;\nuniform float cScale;\nuniform float animateProg;\nuniform bool selectTS;\nuniform vec2 latBounds;\nuniform vec2 lonBounds;\nuniform vec3 nanColor;\nuniform float nanAlpha;\n\n#define pi 3.141592653\n\nvec2 giveUV(vec3 position){\n    vec3 n = normalize(position);\n    float latitude = asin(n.y);\n    float longitude = atan(n.z, n.x);\n    latitude = (latitude - latBounds.x)/(latBounds.y - latBounds.x);\n    longitude = (longitude - lonBounds.x)/(lonBounds.y - lonBounds.x);\n\n    return vec2(1.-longitude, latitude);\n}\n\nvoid main(){\n    vec2 sampleCoord = giveUV(aPosition);\n    float strength = texture(map, sampleCoord).r;\n    bool isNaN = strength == 1.;\n    strength = isNaN ? strength : (strength - 0.5)*cScale + 0.5;\n    strength = isNaN ? strength : min(strength+cOffset,0.99);\n    color = isNaN ? vec4(nanColor, nanAlpha) : texture(cmap, vec2(strength, 0.5));\n    if (!isNaN){\n        color.a = 1.;\n    }\n\n\n}":" // by Jeran Poehls\nprecision highp float;\nprecision highp sampler3D;\n\nout vec4 color;\n\nin vec3 aPosition;\n\nuniform sampler3D map;\nuniform sampler2D cmap;\n\nuniform float cOffset;\nuniform float cScale;\nuniform float animateProg;\nuniform vec4[10] selectBounds; \nuniform bool selectTS;\nuniform vec2 latBounds;\nuniform vec2 lonBounds;\nuniform vec3 nanColor;\nuniform float nanAlpha;\n\n#define pi 3.141592653\n\nvec2 giveUV(vec3 position){\n    vec3 n = normalize(position);\n    float latitude = asin(n.y);\n    float longitude = atan(n.z, n.x);\n    latitude = (latitude - latBounds.x)/(latBounds.y - latBounds.x);\n    longitude = (longitude - lonBounds.x)/(lonBounds.y - lonBounds.x);\n\n    return vec2(1.-longitude, latitude);\n}\n\nbool isValid(vec2 sampleCoord){\n    for (int i = 0; i < 10; i++){\n        vec4 thisBound = selectBounds[i];\n        if (thisBound.x == -1.){\n            return false;\n        }\n        bool cond = (sampleCoord.x < thisBound.r || sampleCoord.x > thisBound.g || sampleCoord.y < thisBound.b ||  sampleCoord.y > thisBound.a);\n        if (!cond){\n            return true;\n        }\n    }\n    return false;\n}\n\nvoid main(){\n    vec2 sampleCoord = giveUV(aPosition);\n    bool inBounds = all(greaterThanEqual(sampleCoord, vec2(0.0))) && \n                all(lessThanEqual(sampleCoord, vec2(1.0)));\n    \n    if (inBounds) {\n    float strength = texture(map, vec3(sampleCoord, animateProg)).r;\n    bool isNaN = strength == 1.;\n    strength = isNaN ? strength : (strength)*cScale;\n    strength = isNaN ? strength : min(strength+cOffset,0.99);\n    color = isNaN ? vec4(nanColor, nanAlpha) : texture(cmap, vec2(strength, 0.5));\n    if (!isNaN){\n        color.a = 1.;\n    }\n    bool cond = isValid(sampleCoord);\n    if (!cond && selectTS){\n        color.rgb *= 0.65;\n    }\n    } else {\n        color = vec4(nanColor, 1.); // Black\n        color.a = nanAlpha;\n    }\n    \n    // color = vec4(sampleCoord, 0., 1.0);\n\n}",blending:o.NTi,side:o.hB5,transparent:!0,depthWrite:!1}),[r]),q=Z.clone();return q.side=o.hsX,(0,s.useEffect)(()=>{if(Z){let e=Z.uniforms;e.map.value=n,e.selectTS.value=C,e.selectBounds.value=N,e.cmap.value=i,e.cOffset.value=I,e.cScale.value=D,e.animateProg.value=w,e.latBounds.value=new o.I9Y(eH(G[0]),eH(G[1])),e.lonBounds.value=new o.I9Y(eH(U[0]),eH(U[1])),e.nanColor.value=new o.Q1f(j),e.nanAlpha.value=1-X}if(q){let e=q.uniforms;e.map.value=n,e.selectTS.value=C,e.selectBounds.value=N,e.cmap.value=i,e.cOffset.value=I,e.cScale.value=D,e.animateProg.value=w,e.latBounds.value=new o.I9Y(eH(G[0]),eH(G[1])),e.lonBounds.value=new o.I9Y(eH(U[0]),eH(U[1])),e.nanColor.value=new o.Q1f(j),e.nanAlpha.value=1-X}},[n,w,i,I,D,k,N,C,U,G,j,X]),(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)("mesh",{renderOrder:1,geometry:F,material:Z,onClick:e=>C&&function(e){let n=function(e,n,t){let i=Math.atan2(e.z,e.x),r=Math.asin(e.y),a=(i-eH(t[0]))/(eH(t[1])-eH(t[0])),l=(r-eH(n[0]))/(eH(n[1])-eH(n[0]));return new o.I9Y(1-a,l)}(e.point.normalize(),G,U),i=new o.Pq0(0,0,1);if(t){var r,a;let e=(0,p.PE)({data:x?S:(0,p.Td)(),shape:_,stride:b},{uv:n,normal:i}),t=i.toArray().map((e,n)=>Math.abs(e)>0?n:null).filter(e=>null!==e);l(2-t[0]);let s=(0,p.jT)({normal:i,uv:n}).map((e,n)=>e?g[n][Math.round(e*g[n].length)]:null),d=v.filter((e,n)=>null!==s[n]),c=z.filter((e,n)=>null!==s[n]);s=s.filter(e=>null!==e);let f="".concat(s[0],"_").concat(s[1]),h={color:(0,y.md)(A()/10,"Paired"),data:e};L(),m({[f]:h}),u({[f]:{first:{name:d[0],loc:null!=(r=s[0])?r:0,units:c[0]},second:{name:d[1],loc:null!=(a=s[1])?a:0,units:c[1]},plot:{units:z[2-t[0]]}}}),function(e,n){let t=Math.floor(e.x*R)+.5,i=Math.ceil(e.y*E)-.5,r=1/R,a=1/E,l={[n]:new o.IUQ(...[t/R-r/2,t/R+r/2],...[i/E-a/2,i/E+a/2])};V(e=>({...l,...e}))}(n,f)}}(e)}),(0,a.jsx)("mesh",{renderOrder:0,geometry:F,material:q})]})};function e$(e,n,t){let[i,r]=e,a=(i-n[0])/(n[1]-n[0]),o=(r-t[0])/(t[1]-t[0]);return a-=.5,o-=.5,[a*=2,(o*=2)/2,0]}function eJ(e){let[n,t]=e,i=t*Math.PI/180,r=n*Math.PI/180;return[Math.cos(i)*Math.cos(r)*1.001,1.001*Math.sin(i),Math.cos(i)*Math.sin(r)*1.001]}function eK(e){let{features:n}=e,{xRange:t,yRange:i,plotType:r,borderColor:l,lonExtent:u,latExtent:x,lonResolution:p,latResolution:y}=(0,c.Ws)((0,f.k)(e=>({xRange:e.xRange,yRange:e.yRange,plotType:e.plotType,borderColor:e.borderColor,lonExtent:e.lonExtent,latExtent:e.latExtent,lonResolution:e.lonResolution,latResolution:e.latResolution}))),{flipY:S,shape:g}=(0,c.on)((0,f.k)(e=>({flipY:e.flipY,shape:e.shape}))),[v,z]=(0,s.useMemo)(()=>{let e=y/2,n=p/2,t=[Math.max(u[0]-n,-180),Math.min(u[1]+n,180)],i=[Math.max(x[0]-e,-90),Math.min(x[1]+e,90)];return[t,i=S?[i[1],i[0]]:i]},[x,u,p,y]),[h,_]=(0,s.useState)(!1);(0,s.useEffect)(()=>{"sphere"===r?_(!0):_(!1)},[r]);let b=(0,s.useMemo)(()=>new o.BKk({glslVersion:o.Wdf,vertexShader:d,fragmentShader:"precision highp float;\r\nprecision highp sampler3D;\r\n\r\nout vec4 color;\r\n\r\nin vec3 aPosition;\r\n\r\nuniform vec2 xBounds;\r\nuniform vec2 yBounds;\r\nuniform vec3 borderColor;\r\nuniform bool trim;\r\n\r\nvoid main() {\r\n\r\n    if ((aPosition.x < xBounds.x || aPosition.x > xBounds.y || aPosition.y < yBounds.x || aPosition.y > yBounds.y) && trim){\r\n        discard;\r\n    }\r\n\r\n    color = vec4(borderColor, 1.0);\r\n}",uniforms:{xBounds:{value:new o.I9Y(t[0],t[1])},yBounds:{value:new o.I9Y(i[0]/g.x,i[1]/g.x)},borderColor:{value:new o.Q1f(l)},trim:{value:!h}}}),[]);(0,s.useEffect)(()=>{if(b){let e=b.uniforms;e.xBounds.value=new o.I9Y(t[0],t[1]),e.yBounds.value=new o.I9Y(i[0]/g.x,i[1]/g.x),e.borderColor.value=new o.Q1f(l),e.trim.value=!h,(0,m.m)()}},[t,i,l,h]);let k=(0,s.useMemo)(()=>n.flatMap((e,n)=>{let t=[];if("LineString"===e.geometry.type){let n=[];e.geometry.coordinates.forEach(e=>{let[t,i]=e,[r,a,l]=h?eJ([-t,i]):e$([t,i],v,z);n.push(new o.Pq0(r,a,l))});let i=new Float32Array(3*n.length);n.forEach((e,n)=>{i.set([e.x,e.y,e.z],3*n)});let r=new o.LoY;r.setAttribute("position",new o.THS(i,3)),t.push(r)}else"MultiPolygon"===e.geometry.type?e.geometry.coordinates.forEach((e,n)=>{let i=n,r=e[0],a=[];r.forEach(e=>{let[n,t]=e;i++;let[r,l,u]=h?eJ([-n,t]):e$([n,t],v,z);a.push(new o.Pq0(r,l,u))});let l=new Float32Array(3*a.length);a.forEach((e,n)=>{l.set([e.x,e.y,e.z],3*n)});let u=new o.LoY;u.setAttribute("position",new o.THS(l,3)),t.push(u)}):("Polygon"===e.geometry.type?[e.geometry.coordinates]:e.geometry.coordinates).forEach(e=>{let n=[];e.forEach(e=>{e.forEach(e=>{let[t,i]=e,[r,a,l]=h?eJ([-t,i]):e$([t,i],v,z);n.push(new o.Pq0(r,a,l))})});let i=new Float32Array(3*n.length);n.forEach((e,n)=>{i.set([e.x,e.y,e.z],3*n)});let r=new o.LoY;r.setAttribute("position",new o.THS(i,3)),t.push(r)});return t}),[n,h,S,v,z]),w=(0,s.useMemo)(()=>k.map((e,n)=>{let t=new o.N1A(e,b);return(0,a.jsx)("primitive",{object:t},"border-".concat(n))}),[k,b]);return(0,a.jsx)(a.Fragment,{children:w})}let e0=()=>{let[e,n]=(0,s.useState)(null),[t,i]=(0,s.useState)(null),[r,o]=(0,s.useState)(!1),{dataShape:l,is4D:u}=(0,c.on)((0,f.k)(e=>({dataShape:e.dataShape,is4D:e.is4D}))),{zRange:d,plotType:x,showBorders:p,timeScale:y}=(0,c.Ws)((0,f.k)(e=>({zRange:e.zRange,plotType:e.plotType,showBorders:e.showBorders,timeScale:e.timeScale}))),{analysisMode:S,axis:g}=(0,c.r2)((0,f.k)(e=>({analysisMode:e.analysisMode,axis:e.axis}))),[v,z]=(0,s.useState)(!1);(0,s.useEffect)(()=>{"sphere"===x?z(!0):z(!1)},[x]),(0,m.D)(e=>{let{camera:n}=e;!v&&(Math.abs(n.rotation.z)>Math.PI/2?o(!0):r&&o(!1))}),(0,s.useEffect)(()=>{fetch("./ne_110m_coastline.json").then(e=>e.json()).then(e=>n(e.features)),fetch("./ne_110m_admin_0_countries.json").then(e=>e.json()).then(e=>i(e.features))},[]);let h="point-cloud"==x,_=l[0]/l[2]*y;return(0,a.jsxs)("group",{visible:p&&!(S&&0!=g),position:v?[0,0,0]:[0,0,r?d[0]*(h?_:1):d[1]*(h?_:1)],children:[e&&(0,a.jsx)(eK,{features:e}),t&&(0,a.jsx)(eK,{features:t})]})};var e2=t(8004),e3=t(1409),e1=t(2821);let e5=e=>{let{flipX:n,flipY:t,flipDown:i}=e,{dimArrays:r,dimNames:l,dimUnits:u,dataShape:d}=(0,c.on)((0,f.k)(e=>({dimArrays:e.dimArrays,dimNames:e.dimNames,dimUnits:e.dimUnits,dataShape:e.dataShape}))),{xRange:m,yRange:x,zRange:y,plotType:S,timeScale:g,animProg:v}=(0,c.Ws)((0,f.k)(e=>({xRange:e.xRange,yRange:e.yRange,zRange:e.zRange,plotType:e.plotType,timeScale:e.timeScale,animProg:e.animProg}))),{hideAxis:z,hideAxisControls:_}=(0,c.qi)((0,f.k)(e=>({hideAxis:e.hideAxis,hideAxisControls:e.hideAxisControls}))),b=[r[0].length,r[1].length,r[2].length],[k,w]=(0,s.useState)(7),[I,D]=(0,s.useState)(7),[C,M]=(0,s.useState)(7),O=(0,s.useMemo)(()=>"point-cloud"==S,[S]),P=O?d[2]/500:1,Y=(0,s.useMemo)(()=>d[0]/d[2]*g,[d,g]),j=(0,s.useMemo)(()=>d[1]/d[2],[d]),X=(0,h.$m)("--text-plot"),A=(0,s.useMemo)(()=>{if(X)return new o.Q1f(X).getHex()},[X]),L=(0,s.useMemo)(()=>new e1.G({color:A||0,linewidth:2}),[A]),B=.05*P,V=(0,s.useMemo)(()=>{let e=new e2.n().setPositions([m[0]*P-B/2,0,0,m[1]*P+B/2,0,0]);return new e3.b(e,L)},[m,L,P]),T=(0,s.useMemo)(()=>{let e=new e2.n().setPositions([0,x[0]*j*P,0,0,x[1]*j*P+B/2,0]);return new e3.b(e,L)},[x,j,L,P]),E=(0,s.useMemo)(()=>{let e=new e2.n().setPositions([0,0,O?y[0]*P*Y-B/2:y[0]-B/2,0,0,O?y[1]*P*Y+B/2:y[1]+B/2]);return new e3.b(e,L)},[y,Y,O,L,P]),R=(0,s.useMemo)(()=>{let e=new e2.n().setPositions([0,0,0,0,0,B]);return new e3.b(e,L)},[L,P]),U=k/(k-1),G=1/(k-1),F=I/(I-1),Z=1/(I-1),q=C/(C-1),W=1/(C-1);return(0,a.jsxs)("group",{visible:"sphere"!=S&&"flat"!=S&&!z,children:[(0,a.jsxs)("group",{position:[0,O?j*P*x[0]:j*x[0],0],children:[(0,a.jsxs)("group",{position:[0,0,n?O?y[0]*Y*P-B/2:y[0]-B/2:O?y[1]*P*Y+B/2:y[1]+B/2],rotation:[i?n?-Math.PI/2:Math.PI/2:0,0,0],children:[(0,a.jsx)("primitive",{object:V},"xLine"),Array(k).fill(null).map((e,t)=>(m[0]+1)/2<=t*U/k&&(m[1]+1)/2>=t*U/k&&(0,a.jsxs)("group",{position:[O?-P+t*U/(k/2)*P:-1+t*U/(k/2),0,0],children:[(0,a.jsx)("primitive",{object:R.clone(),rotation:[0,n?Math.PI:0,0]},t),(0,a.jsx)(N.E,{anchorX:0==t?n?"right":"left":t==k-1?n?"left":"right":"center",anchorY:"top",fontSize:.05*P,color:A,"material-depthTest":!1,rotation:[-Math.PI/2,0,n?Math.PI:0],position:[0,0,n?-.05*P:.05*P],children:(0,p.SB)(r[2][Math.floor((b[2]-1)*t*G)],u[2])},"textX_".concat(t))]},"xGroup_".concat(t))),(0,a.jsxs)("group",{rotation:[-Math.PI/2,0,n?Math.PI:0],position:[(m[0]+m[1])/2*P,0,n?-.2*P:.2*P],children:[(0,a.jsx)(N.E,{anchorX:"center",anchorY:"top",fontSize:.1*P,color:A,"material-depthTest":!1,children:l[2]},"xTitle"),(0,a.jsxs)("group",{visible:!_,children:[k<20&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.2*P,color:A,"material-depthTest":!1,position:[.2*P,-.2*P,0],onClick:e=>w(e=>Math.min(e+1,20)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"+"},"xAdd"),k>1&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.2*P,color:A,"material-depthTest":!1,position:[-.2*P,-.2*P,0],onClick:e=>w(e=>Math.max(e-1,1)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"-"},"xSub")]})]})]}),(0,a.jsxs)("group",{position:[t?m[1]*P+B/2:m[0]*P-B/2,0,0],rotation:[0,0,i?t?-Math.PI/2:Math.PI/2:0],children:[(0,a.jsx)("primitive",{object:E},"zLine"),Array(C).fill(null).map((e,n)=>(y[0]+1)/2<=n*q/C&&(y[1]+1)/2>=n*q/C&&(0,a.jsxs)("group",{position:[0,0,O?-Y*P+n*q/(C/2)*Y*P:-1+n*q/(C/2)],children:[(0,a.jsx)("primitive",{object:R.clone(),rotation:[0,t?Math.PI/2:-Math.PI/2,0]},n),(0,a.jsx)(N.E,{anchorX:0==n?t?"right":"left":n==C-1?t?"left":"right":"center",anchorY:"top",fontSize:.04*P,color:A,"material-depthTest":!1,rotation:[-Math.PI/2,0,t?Math.PI/2:-Math.PI/2],position:[t?.05*P:-.05*P,0,0],children:(0,p.SB)(r[0][(Math.floor((b[0]-1)*n*W)+Math.floor(b[0]*v))%b[0]],u[0])},"textY_".concat(n))]},"zGroup_".concat(n))),(0,a.jsxs)("group",{rotation:[-Math.PI/2,0,t?Math.PI/2:-Math.PI/2],position:[t?.2*P:-.2*P,0,O?(y[0]+y[1])/2*Y*P:(y[0]+y[1])/2],children:[(0,a.jsx)(N.E,{anchorX:"center",anchorY:"top",fontSize:.1*P,color:A,"material-depthTest":!1,children:l[0]},"zTitle"),(0,a.jsxs)("group",{visible:!_,children:[C<20&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.2*P,color:A,"material-depthTest":!1,position:[.2*P,-.2*P,0],onClick:e=>M(e=>Math.min(e+1,20)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"+"},"zAdd"),C>1&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.2*P,color:A,"material-depthTest":!1,position:[-.2*P,-.2*P,0],onClick:e=>M(e=>Math.max(e-1,1)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"-"},"zSub")]})]})]})]}),(0,a.jsxs)("group",{position:[t?m[0]*P-B/2:m[1]*P+B/2,0,n?O?y[0]*Y*P-B/2:y[0]-B/2:O?y[1]*Y*P+B/2:y[1]+B/2],children:[(0,a.jsx)("primitive",{object:T},"yLine"),Array(I).fill(null).map((e,i)=>(x[0]+1)/2<=i*F/I&&(x[1]+1)/2>=i*F/I&&(0,a.jsxs)("group",{position:[0,O?-j*P+i*F/(I/2)*j*P:-j+i*F/(I/2)*j,0],children:[(0,a.jsx)("primitive",{object:R.clone(),rotation:[0,t?-Math.PI/2:Math.PI/2,0]},i),(0,a.jsx)(N.E,{anchorX:t?n?"left":"right":n?"right":"left",anchorY:"middle",fontSize:.05*P,color:A,"material-depthTest":!1,rotation:[0,n?Math.PI:0,0],position:[t?-.07*P:.07*P,0,0],children:(0,p.SB)(r[1][Math.floor((b[1]-1)*i*Z)],u[1])},"text_".concat(i))]},"yGroup_".concat(i))),(0,a.jsxs)("group",{rotation:[0,n?Math.PI:0,0],position:[t?-.25*P:.25*P,(x[0]+x[1])/2*j*P,0],children:[(0,a.jsx)(N.E,{anchorX:t?n?"left":"right":n?"right":"left",anchorY:"middle",fontSize:.1*P,color:A,"material-depthTest":!1,children:l[1]},"yTitle"),(0,a.jsxs)("group",{visible:!_,children:[I<20&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.2*P,color:A,"material-depthTest":!1,position:[t==n?.2*P:-.2*P,.2*P,0],onClick:e=>D(e=>Math.min(e+1,20)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"+"},"zAdd"),I>1&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.2*P,color:A,"material-depthTest":!1,position:[t==n?.2*P:-.2*P,-.2*P,0],onClick:e=>D(e=>Math.max(e-1,1)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"-"},"zSub")]})]})]})]})},e4=()=>{let{dimArrays:e,dimNames:n,dimUnits:t}=(0,c.on)((0,f.k)(e=>({dimArrays:e.dimArrays,dimNames:e.dimNames,dimUnits:e.dimUnits}))),{plotType:i}=(0,c.Ws)((0,f.k)(e=>({plotType:e.plotType}))),{hideAxis:r,hideAxisControls:l}=(0,c.qi)((0,f.k)(e=>({hideAxis:e.hideAxis,hideAxisControls:e.hideAxisControls}))),{analysisMode:u,axis:d}=(0,c.r2)((0,f.k)(e=>({analysisMode:e.analysisMode,axis:e.axis}))),m=2==e.length,x=(0,s.useMemo)(()=>u&&!m?e.filter((e,n)=>n!=d).map(e=>e.length):e.map(e=>e.length),[d,e,u]),y=(0,s.useMemo)(()=>u&&2==d&&!m,[d,u]),S=y?x.length-2:x.length-1,g=y?x.length-1:x.length-2,[v,z]=(0,s.useState)(7),[_,b]=(0,s.useState)(7),{axisArrays:k,axisUnits:w,axisNames:I}=(0,s.useMemo)(()=>u&&!m?{axisArrays:e.filter((e,n)=>n!=d),axisUnits:t.filter((e,n)=>n!=d),axisNames:n.filter((e,n)=>n!=d)}:{axisArrays:e,axisUnits:t,axisNames:n},[u,e,t,n]),D=(0,s.useMemo)(()=>(u,x[g]/x[S]),[x,u,d]),C=(0,h.$m)("--text-plot"),M=(0,s.useMemo)(()=>{if(C)return new o.Q1f(C).getHex()},[C]),O=(0,s.useMemo)(()=>new e1.G({color:M||0,linewidth:2}),[M]),P=(0,s.useMemo)(()=>{let e=new e2.n().setPositions([-1/(y?D:1)-.025,0,0,1/(y?D:1)+.025,0,0]);return new e3.b(e,O)},[O,y,D]),Y=(0,s.useMemo)(()=>{let e=new e2.n().setPositions([0,-(y?1:D)-.025,0,0,(y?1:D)+.025,0]);return new e3.b(e,O)},[D,O,y,D]),j=(0,s.useMemo)(()=>{let e=new e2.n().setPositions([0,0,0,0,0,.05]);return new e3.b(e,O)},[O,y]),X=v/(v-1),A=1/(v-1),L=_/(_-1),B=1/(_-1);return(0,a.jsxs)("group",{visible:"flat"==i&&!r,children:[(0,a.jsxs)("group",{position:[0,-(y?1:D)-.025,0],rotation:[Math.PI/2,0,0],children:[(0,a.jsx)("primitive",{object:P},"xLine"),Array(v).fill(null).map((e,n)=>v>1&&(0,a.jsxs)("group",{position:[-(y?1/D:1)+n*X/(v/2)*(y?1/D:1),0,0],children:[(0,a.jsx)("primitive",{object:j.clone(),rotation:[0,0,0]},n),(0,a.jsx)(N.E,{anchorX:"center",anchorY:"top",fontSize:.05,color:M,"material-depthTest":!1,rotation:[-Math.PI/2,0,0],position:[0,0,.05],children:(0,p.SB)(k[S][Math.floor((x[S]-1)*n*A)],w[S])},"textX_".concat(n))]},"xGroup_".concat(n))),(0,a.jsxs)("group",{rotation:[-Math.PI/2,0,0],position:[0,0,.2],children:[(0,a.jsx)(N.E,{anchorX:"center",anchorY:"top",fontSize:.1,color:M,"material-depthTest":!1,children:I[S]},"xTitle"),(0,a.jsxs)("group",{visible:!l,children:[v<20&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.15,color:M,"material-depthTest":!1,position:[.2,-.15,0],onClick:e=>z(e=>Math.min(e+1,20)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"+"},"xAdd"),v>1&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.15,color:M,"material-depthTest":!1,position:[-.2,-.15,0],onClick:e=>z(e=>Math.max(e-1,1)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"-"},"xSub")]})]})]}),(0,a.jsxs)("group",{position:[-(y?1/D:1)-.025,0,0],children:[(0,a.jsx)("primitive",{object:Y},"yLine"),Array(_).fill(null).map((e,n)=>_>1&&(0,a.jsxs)("group",{position:[0,-(y?1:D)+n*L/(_/2)*(y?1:D),0],rotation:[0,0,Math.PI],children:[(0,a.jsx)("primitive",{object:j.clone(),rotation:[0,Math.PI/2,0]},n),(0,a.jsx)(N.E,{anchorX:"right",anchorY:"middle",fontSize:.05,color:M,"material-depthTest":!1,rotation:[0,0,-Math.PI],position:[.07,0,0],children:(0,p.SB)(k[g][Math.floor((x[g]-1)*n*B)],w[g])},"text_".concat(n))]},"yGroup_".concat(n))),(0,a.jsxs)("group",{rotation:[0,0,0],position:[-.25,0,0],children:[(0,a.jsx)(N.E,{anchorX:"right",anchorY:"middle",fontSize:.1,color:M,"material-depthTest":!1,children:I[g]},"yTitle"),(0,a.jsxs)("group",{visible:!l,children:[_<20&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.15,color:M,"material-depthTest":!1,position:[-.1,.2,0],onClick:e=>b(e=>Math.min(e+1,20)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"+"},"zAdd"),_>1&&(0,a.jsx)(N.E,{anchorX:"center",anchorY:"middle",fontSize:.15,color:M,"material-depthTest":!1,position:[-.1,-.2,0],onClick:e=>b(e=>Math.max(e-1,1)),onPointerEnter:e=>document.body.style.cursor="pointer",onPointerLeave:e=>document.body.style.cursor="default",children:"-"},"zSub")]})]})]})]})},e6=()=>{let[e,n]=(0,s.useState)(!1),[t,i]=(0,s.useState)(!1),[r,o]=(0,s.useState)(!1),{isFlat:l}=(0,c.on)((0,f.k)(e=>({isFlat:e.isFlat})));return(0,m.D)(a=>{let{camera:l}=a,u=Math.abs(l.rotation.z)>Math.PI/2;e!==u&&n(u);let s=l.rotation.z>0&&l.rotation.x<0||l.rotation.z<=0&&l.rotation.x>0;t!==s&&i(s);let d=l.rotation.x>0||l.position.y<=0;r!==d&&o(d)}),(0,a.jsxs)(a.Fragment,{children:[!l&&(0,a.jsx)(e5,{flipX:e,flipY:t,flipDown:r}),(0,a.jsx)(e4,{})]})};var e9=t(3463);let e8=()=>{let e=(0,s.useRef)(null),{gl:n}=(0,m.C)(),{setMaxTextureSize:t,setMax3DTextureSize:i}=(0,c.Ws)((0,f.k)(e=>({setMaxTextureSize:e.setMaxTextureSize,setMax3DTextureSize:e.setMax3DTextureSize})));(0,s.useEffect)(()=>{let e=n.getContext();i(e.getParameter(e.MAX_3D_TEXTURE_SIZE)),t(e.getParameter(e.MAX_TEXTURE_SIZE))},[]);let{colormap:r}=(0,c.on)((0,f.k)(e=>({colormap:e.colormap}))),{spherePositions:l,cubePositions:u,planePositions:d}=(0,s.useMemo)(()=>{let e=new Float32Array(46875),n=new Float32Array(46875),t=new Float32Array(46875),i=Math.PI*(3-Math.sqrt(5));for(let n=0;n<15625;n++){let t=1-n/15624*2,r=Math.sqrt(1-t*t),a=i*n,o=Math.cos(a)*r,l=Math.sin(a)*r;e[3*n]=1.2*o,e[3*n+1]=1.2*t,e[3*n+2]=1.2*l}let r=0;for(let e=0;e<25;e++)for(let t=0;t<25;t++)for(let i=0;i<25;i++)n[3*r]=(e/25-.5)*2,n[3*r+1]=(t/25-.5)*2,n[3*r+2]=(i/25-.5)*2,r++;r=0;for(let e=0;e<125;e++)for(let n=0;n<125;n++)t[3*r]=(e/125-.5)*2.5,t[3*r+1]=(n/125-.5)*2.5,t[3*r+2]=0,r++;return{spherePositions:e,cubePositions:n,planePositions:t}},[15625]),x=(0,s.useMemo)(()=>new o.BKk({glslVersion:o.Wdf,uniforms:{uSphereMix:{value:1},uCubeMix:{value:0},uPlaneMix:{value:0},uTime:{value:0},cmap:{value:r}},vertexShader:"uniform float uSphereMix;\r\nuniform float uCubeMix;\r\nuniform float uPlaneMix;\r\nuniform float uSize;\r\nuniform float uTime;\r\nuniform sampler2D cmap;\r\n\r\nattribute vec3 aSpherePosition;\r\nattribute vec3 aCubePosition;\r\nattribute vec3 aPlanePosition;\r\n\r\nvarying vec3 vColor; \r\n\r\nvoid main() {\r\n    // Linearly interpolate between the three shapes using the mix uniforms\r\n    vec3 pos = mix(aSpherePosition, aCubePosition, uCubeMix);\r\n    pos = mix(pos, aPlanePosition, uPlaneMix);\r\n\r\n    // Add a slight sine wave animation to make it more dynamic\r\n    // pos.y += sin(pos.x * 50.0 + uTime) * 0.005;\r\n    // pos.x += cos(pos.y * 50.0 + uTime) * 0.005;\r\n\r\n    float minBrightness = 0.2;\r\n    float maxBrightness = 0.96;\r\n\r\n    float r = sin(pos.z + (uTime * 0.2 ) )  ;\r\n    float g = cos(pos.y + (uTime * 0.3 ) );\r\n    float b = cos(pos.x+pos.y + (uTime * 0.5));\r\n\r\n    vColor = vec3(r, g, b);\r\n    float mag = min(((sin(r+g+b) + 1.) /2.), 0.996) ;\r\n    vec4 sampled = texture(cmap, vec2(mag, 0.5));\r\n    vColor = sampled.rgb;\r\n    // Calculate luminance (perceived brightness)\r\n\r\n    vec4 modelPosition = modelMatrix * vec4(pos, 1.0);\r\n    vec4 viewPosition = viewMatrix * modelPosition;\r\n    vec4 projectedPosition = projectionMatrix * viewPosition;\r\n\r\n    gl_Position = projectedPosition;\r\n\r\n    // Make points smaller as they are further away (perspective)\r\n    gl_PointSize = (15.0 / -viewPosition.z);\r\n}\r\n\r\n",fragmentShader:"// Fragment Shader\r\n\r\nin vec3 vColor;\r\nout vec4 Color;\r\n\r\nvoid main() {\r\n    // Simple white color for the points\r\n    Color = vec4(vColor, 0.8);\r\n}"}),[]);return(0,s.useEffect)(()=>{let e=null;if(x){let n=x.uniforms;(e=e9.Ay.timeline({repeat:-1,yoyo:!1})).to(n.uCubeMix,{value:1,duration:2,delay:3,ease:"power2.inOut"}),e.to(n.uCubeMix,{value:0,duration:2,delay:3,ease:"power2.inOut"}),e.to(n.uPlaneMix,{value:1,duration:2,ease:"power2.inOut"},"<"),e.to(n.uPlaneMix,{value:0,duration:2,delay:3,ease:"power2.inOut"})}return()=>{e&&e.kill()}},[]),(0,m.D)(n=>{x&&(x.uniforms.uTime.value=n.clock.getElapsedTime()),e.current&&(e.current.rotation.y+=.001,e.current.rotation.x+=.001)}),(0,s.useEffect)(()=>{x&&(x.uniforms.cmap.value=r)},[r]),(0,a.jsx)("points",{ref:e,material:x,children:(0,a.jsxs)("bufferGeometry",{children:[(0,a.jsx)("bufferAttribute",{attach:"attributes-position",args:[l,3],count:15625}),(0,a.jsx)("bufferAttribute",{attach:"attributes-aSpherePosition",args:[l,3],count:15625}),(0,a.jsx)("bufferAttribute",{attach:"attributes-aCubePosition",args:[u,3],count:15625}),(0,a.jsx)("bufferAttribute",{attach:"attributes-aPlanePosition",args:[d,3],count:15625})]})})},e7=()=>(0,a.jsx)("div",{className:"w-[100vw] h-[100vh]",children:(0,a.jsx)(z.Hl,{camera:{position:[0,0,3]},children:(0,a.jsx)(e8,{})})});o.l2R.enabled=!0;let ne=function(){let{initStore:e,timeSeries:n,variable:t,metadata:i,plotOn:r,setZMeta:o,setVariables:d,setPlotOn:m,setTitleDescription:x}=(0,c.on)((0,f.k)(e=>({initStore:e.initStore,timeSeries:e.timeSeries,variable:e.variable,metadata:e.metadata,plotOn:e.plotOn,setZMeta:e.setZMeta,setVariables:e.setVariables,setPlotOn:e.setPlotOn,setTitleDescription:e.setTitleDescription}))),{currentStore:p,setCurrentStore:y}=(0,c.SZ)((0,f.k)(e=>({currentStore:e.currentStore,setCurrentStore:e.setCurrentStore})));(0,s.useEffect)(()=>{e.startsWith("local")||y((0,u.Oe)(e))},[e,y]);let S=(0,s.useMemo)(()=>new u.Y5(p),[p]);return(0,s.useEffect)(()=>{let e=!0;(0,l.tz)(p).then(n=>{e&&x(n)});let n=(0,l.Tk)(p),t=(0,l.Bi)(n);return n.then(e=>o(e)),t.then(e=>d(e)),()=>{e=!1}},[p,o,d,x]),(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(h.oU,{}),"Default"==t&&(0,a.jsx)(e7,{}),(0,a.jsx)(h.$D,{}),!r&&(0,a.jsx)(h.Fp,{}),(0,a.jsx)(h.Rh,{}),"Default"!=t&&(0,a.jsx)(eF,{ZarrDS:S}),i&&(0,a.jsx)(h.OS,{data:i}),Object.keys(n).length>=1&&(0,a.jsx)(V,{})]})}},7181:()=>{},7384:(e,n,t)=>{Promise.resolve().then(t.bind(t,6284))}},e=>{e.O(0,[956,693,197,669,813,444,755,858,56,667,800,645,812,959,152,195,499,100,970,984,65,340,437,769,98,53,225,358],()=>e(e.s=7384)),_N_E=e.O()}]);